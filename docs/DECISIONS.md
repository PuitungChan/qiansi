# 决策记录（DECISIONS）

> **这个文件是这个项目的长期记忆。**
>
> 跨会话的上下文只能靠文件承载。任何重要决定——尤其是「为什么不做某事」——都必须写在这里，
> 否则下次开工时没人（包括 AI）知道。

---

## 使用规则

1. **每个决策一个编号**，格式 `D-XXX`
2. **必须写「理由」和「影响面」**，只写结论等于没写
3. **不删除已废弃的决策**，改状态为「已废弃」并写明被谁取代
4. **状态变更要记日期**

状态：`已决` / `待决` / `已废弃` / `待验证`

---

## 已决

### D-003 · 维度：2D 玩法 + 2.5D 表现

| 项 | 内容 |
|---|---|
| **状态** | 已决 |
| **日期** | 2026-09 |
| **决策** | 物理严格锁定 XY 平面；渲染与相机走 2.5D（7 层视差 + 正交相机 + 轻微滚转） |
| **理由** | 这个游戏的全部深度来自**标量物理量**（动量、动能、张力差）。加 Z 轴只增加操作复杂度，不增加任何机制深度。预判线在 3D 中会退化成落点标记，「读懂物理」的核心体验消失 |
| **影响面** | 全部渲染需求（FR-RND-*）、相机行为（FR-MTA-004）、操作方案（FR-ACT-*） |
| **依据** | `docs/牵丝_游戏设计方案.md` §9.5、`prototype/layers_25d_spec.png` |
| **代价** | 需要实现视差层系统；但换来 2D 物理的稳定与确定性 |

### D-004 · 多人：明确排除，不是延期

| 项 | 内容 |
|---|---|
| **状态** | 已决 |
| **日期** | 2026-09 |
| **决策** | 本作不含实时多人对战或合作。不引入帧同步、房间服务、匹配服务 |
| **理由** | 核心是「读懂物理然后一击」的单人心智活动。加入实时同步会**同时**把 1.5s 重凝、凝神减速、确定性回放三件事变复杂，收益与成本严重不匹配 |
| **影响面** | FR-MP-001~003（P0 排除性需求）；架构上不需要网络同步层 |
| **替代方案** | 异步竞争：排行榜 + 回放对比（FR-MP-002） |

### D-005 · 挑战模式是主玩法，不是通关后解锁内容

| 项 | 内容 |
|---|---|
| **状态** | 已决 |
| **日期** | 2026-09 |
| **决策** | 「墨渊试炼」提为 **P0 / R2**，与主线平级 |
| **理由** | 微信小游戏生态里，可重复游玩 + 排行榜 + 社交比较才是留存引擎。原设计中把它放在「通关后解锁」会浪费掉最大的留存资源 |
| **影响面** | FR-CHL-001~006；R2 范围扩大；排行榜与重放校验成为必需（FR-CHL-005） |
| **依赖** | 需要确定性内核（FR-PHY-008）先成立 |

### D-006 · 广告：预留接口，默认关闭

| 项 | 内容 |
|---|---|
| **状态** | 已决 |
| **日期** | 2026-09 |
| **决策** | 预留 `IAdService` 抽象层 + `NullAdService` 默认实现；所有广告位由服务端配置控制，**默认全关**；序章与第一章禁投 |
| **理由** | 「预留接口」与「上线广告」是两件事。留抽象层避免后期重构；但广告一旦进入核心循环，会摧毁「卡关 → 自己想」的学习循环（与 AC-01 直接冲突） |
| **影响面** | 新增 FR-ADS-001~016、CR-ADS-001~007；**改变版号路径**（见 D-007） |
| **硬验收** | AC-44：删除整个广告模块后，游戏必须仍能编译并通关 |
| **依据** | `docs/SRS_牵丝_v1.0.md` §6.17、§8.7 |

### D-011 · 命名系统是必需品，不是锦上添花

| 项 | 内容 |
|---|---|
| **状态** | 已决 |
| **日期** | 2026-09 |
| **决策** | 高价值物理事件必须触发慢镜 + 毛笔字命名（FR-NAM-001） |
| **理由** | 物理沙盒的深度是**涌现**的，而涌现是**不可见**的。玩家做出精彩操作却以为是运气。命名系统是把「涌现」翻译成「体验」的唯一手段 |
| **影响面** | FR-NAM-001~005；视觉规范 §8 |

### D-012 · R1 阶段零美术

| 项 | 内容 |
|---|---|
| **状态** | 已决 |
| **日期** | 2026-09 |
| **决策** | M0 与 M1 全部使用灰盒，不投入任何美术与音频资源 |
| **理由** | 手感不成立的话，水墨再美也没用。M1 的判定点是 AC-01（自主解法率），不是画面 |
| **影响面** | 美术方案可推迟到 R2 决定；R1 现金支出接近零 |

### D-017 · 物理内核自研，Cocos 只负责渲染与输入 ★

| 项 | 内容 |
|---|---|
| **状态** | 已决（**待创始人追认**，见文末变更日志） |
| **日期** | 2026-09 |
| **决策** | 刚体求解、丝线、伤害、确定性步进全部由**零引擎依赖的纯 TypeScript 内核**实现；Cocos Creator 只承担渲染、输入采集、资源与打包 |
| **理由** | 三条硬理由：① **AC-05（确定性）是 R1 的两条存续判定点之一**，而 Cocos 内置 Box2D 的固定步长由 `PhysicsSystem2D` 累加器驱动，我在没有编辑器的情况下**无法验证**它跨设备是否逐位一致；自研内核可以在本机用 Node 跑确定性回归，把 AC-05 从"希望"变成"证据"。② 本机没有 Cocos Creator，若物理逻辑依赖 `cc`，则**整条 M0 交付物一行都验证不了**。③ 内核与引擎解耦后，万一引擎路线变更，迁移成本只剩渲染层 |
| **影响面** | 与 SRS §2.3「物理：Box2D（刚体）+ 自研 Verlet（绳索）」**存在偏差**，须走 RTM 影响分析后更新 SRS；NFR-MNT-004（单元测试）变得可行且成为交付物 |
| **代价** | 自研求解器只覆盖 M0/R1 需要的形状与约束（圆 + 轴对齐盒、无自转、顺序冲量求解）。柱体滚转、关节、缠绕等后续需求可能需要重新引入 Box2D，届时通过 `PhysicsBackend` 接口替换 |
| **回退方案** | 若创始人否决：改为直接调用 Cocos `PhysicsSystem2D`，代价是**放弃本机验证能力与确定性自证**，M0 只能由真机试玩来发现问题 |

### D-018 · 单位与坐标系

| 项 | 内容 |
|---|---|
| **状态** | 已决（**待创始人追认**） |
| **决策** | 1 物理单位 = **1 米**；Y 轴向上；重力 `g = (0, -20)` m/s²；渲染比例 **60 px/m**（1920×1080 逻辑分辨率 ⇒ 32m × 18m 房间） |
| **理由** | 设计文档给的全部常数（重力 20、丝长 6/12、收丝 8、质量表）都是米制语义。60 px/m 下丝长 6m = 360px（约屏宽 19%），视觉上"一根丝横跨小半个房间"，与原型图比例接近 |
| **影响面** | 全部关卡尺寸、相机 orthographicSize、UI 逻辑像素（FR-ACT-009 的 22 逻辑像素判定半径） |

### D-019 · 主角着地/离地的实现方式

| 项 | 内容 |
|---|---|
| **状态** | 已决 |
| **决策** | 主角**始终是 dynamic 刚体**；着地时质量抬到 `1e6`（等效 ∞），离地立刻恢复 `0.5`。着地判定 = 与静态碰撞体接触且接触法线 `ny > 0.7` |
| **理由** | 保留与地面/斜坡/被重物撞击的真实碰撞响应，后续关卡地形不用重写；"着地甩得动重物、离地自己被拉走"由质量差自然涌现，不需要特判 |
| **影响面** | FR-PHY-003 / AC-20；主角移动采用"着地目标速度控制"（见 D-020） |
| **风险** | 质量突变会让"落地的瞬间"产生一次动量跳变。这是设计意图（着地 = 变成锚点），但需真机确认不出现被丝"弹回"的错觉。参数 `GROUNDED_MASS` 留作调参 |

### D-020 · 主角移动与丝线连接语义

| 项 | 内容 |
|---|---|
| **状态** | 已决 |
| **决策** | ① 主角**移动**由输入直接驱动目标速度（着地 6 m/s，离地 1.5 m/s 空气控制），通过有上限的加速度逼近，不使用力；② **连接瞬间丝长 = 当时两点距离**（不产生任何拉力），只有玩家主动收丝才产生张力；③ 收/放丝改变的是"目标丝长"，速率 8 m/s（素丝）/ 14 m/s（疾丝），范围 0.5–12 m |
| **理由** | ①"角色控制器 + 目标速度"是 2D 平台游戏的标准做法，手感可预期且与质量解耦；② 若连接瞬间固定 6m，则石头摆放在 6m 内时丝是松弛的，玩家第一次连上会觉得"没反应"，而设计 §7 明确要求"连上后石头微微晃动"；③ 满足铁律 1（松弛不传力） |
| **影响面** | FR-PHY-002/011 / FR-ACT-001~005 / AC-19；M0 房间的物件摆位 |

### D-021 · 张力模型（**已被 D-028 / D-029 修订**）

| 项 | 内容 |
|---|---|
| **状态** | **已废弃**（模型保留，常数被 D-028 / D-029 取代） |
| **日期** | 2026-09（初版）→ 2026-09（实测后废弃常数部分） |
| **初版决策** | 张力是有量纲的力：`T = k·ΔL + c·Δv_axial`（`ΔL ≤ 0` 时 `T = 0`，对应"松弛不传力"）。初版取 `k = 1600 N/m`、`ζ = 0.5`、`T_max = 400 N`（素丝）/ `700 N`（韧丝） |
| **初版的漂亮推论（后被实测推翻）** | `T_max = 400 N` 恰好等于**墨甲（质量 20）的重力** `20 × 20 = 400 N`。素丝吊不起墨甲、韧丝可以——这解释了为什么墨甲的弱点是"动量 mv"而不是"被拉住"。当时把这当作"400 就是牛顿"的直接证据 |
| **为什么被推翻** | 把 400 N 当作真实断裂载荷后，实测定标发现：**玩家只是按住收丝、或向左跑开把丝拉直，张力就会瞬间冲到 750+ / 480 N**，丝在 2–90 个 tick 内自己绷断。连"移动"和"牵着石头走"这种最基础的操作都会断丝，游戏无法进行。证据与重新定标过程见 **D-028（刚度与阻尼）** 与 **D-029（显示刻度 vs 物理载荷）** |
| **保留下来的部分** | ① 张力是弹簧-阻尼模型、单位是牛顿；② **松弛（ΔL ≤ 0）时张力恒为 0** —— 这是铁律 1「丝只能拉不能推」在代码里的唯一落点，被 `tests/rope.test.ts` 锁死；③ 张力分档（60% / 85% / 95%）按**占额定值的比例**计算，因此与常数怎么标定无关 |
| **教训（写给未来的自己）** | "数值上的巧合"不是"设计意图的证据"。一个漂亮的等式（400 = 墨甲自重）值得记下来当假设，但不能拿它替代实测定标——幸好这一点在 M0 阶段就被发现，而不是在 M1 的真机试玩里 |

### D-022 · 伤害结算：按目标弱点择一，不叠加

| 项 | 内容 |
|---|---|
| **状态** | 已决 |
| **决策** | 一次撞击**只结算一条伤害公式**，选取目标弱点对应的那一条：`any`（墨卒）取两者较高值、`impact`（墨甲）只结算冲击、`cut`（墨刃）只结算切割、`tear`（墨缚）/`structure`（墨巢）走各自的专用规则 |
| **理由** | 若两条公式相加，附录 A/B 立刻自相矛盾：石块(4)@25m/s 打墨甲 = 冲击 25 + 切割 10.4 = 35.4 ≥ 30 HP，**一次就死**，而附录 B 明确写"墨甲需要两次石块投掷"。择一后单次冲击 = 4×25/4 = 25 < 30，两次击杀成立，与附录 B 完全自洽 |
| **影响面** | FR-CBT-002/003 / AC-02 |
| **回归护栏** | `tests/damage.test.ts` 用附录 A / 附录 B / 设计 §4.1 的**每一个参考算例**做断言，其中一条专门断言"若叠加就会破坏附录 B" |

### D-023 · 附录 A/B 数值口径复核结论

| 项 | 内容 |
|---|---|
| **状态** | **待创始人拍板** |
| **发现** | 附录 A 的算例 `石块(4) @ 20m/s → 冲击 20` 与公式 `min(4,20)×20/4 = 20` 一致；但附录 B 写"墨甲 HP 30，需要两次石块投掷"，两次冲击 = 40 ≥ 30 成立，**单次 20 < 30 不致死**，口径自洽。真正需要澄清的是**速度口径**：`v_rel` 是撞击瞬间的相对速度，而玩家能甩出的速度上限受张力模型（D-021）约束——`r=6m` 时理论峰值约 `v=24.5 m/s`（`T=400N` 的离心力上限），对应冲击 24.5，仍是两次 |
| **结论** | 附录 A/B **无需修改**。M0 将"石块@20m/s 打墨甲造成 20 伤害、两次击杀"写入自动化测试作为回归基线 |
| **待确认点** | 敌人是否受击后有硬直/位移（文档未写）。M0 暂定：受击仅扣血 + 轻微击退（与来袭动量成正比），无硬直 |

### D-024 · 预判线 = "假设此刻断丝"的重力弹道

| 项 | 内容 |
|---|---|
| **状态** | 已决 |
| **决策** | 预判线用**与主循环完全相同的积分器**前向模拟 18 步（0.3s），初始条件 = 被牵物体当前的位置与速度，**只含重力、不含丝线约束、不含碰撞**，采样成点列后以金色点线绘制 |
| **理由** | 玩家真正要预测的是"我断丝的瞬间它会飞向哪"，因此必须按**断丝后**的自由弹道算，而不是按"丝还连着"算。不含碰撞满足 FR-UI-003"不显示碰撞后反弹"；用同一积分器保证预判线与真实结果零漂移 |
| **影响面** | FR-UI-003 / AC-02。R2 的伤害预读（FR-UI-004）复用同一套前向模拟 |

### D-025 · M0 刚体不做自转

| 项 | 内容 |
|---|---|
| **状态** | 已决（M0 范围内） |
| **决策** | M0 的所有刚体（主角/石块/墨甲/地形）**没有角速度与转动惯量**，形状只用**圆**与**轴对齐矩形** |
| **理由** | 核心机制（角动量守恒）发生在"物体绕主角旋转"的**轨道**层面，而不是刚体自转；M0 验证的是"甩动爽不爽"，自转只增加求解器复杂度与不确定性。取消自转后确定性更容易证明，且可本地回归 |
| **影响面** | 石块不会"滚动"，在地面上是滑动；灰盒阶段可接受。若真机试玩觉得"石头滚起来才有分量"，R2 再引入自转（已列入风险） |

### D-026 · M0 敌人行为：纯靶子 + 缓慢游走

| 项 | 内容 |
|---|---|
| **状态** | 已决 |
| **决策** | M0 的墨甲**不追击、不攻击**，只在一个水平区间内以 0.8 m/s 缓慢往返。M0 不实现死亡/重开流程，墨甲血量归零时仅播放灰盒反馈并标记 |
| **理由** | M0 的三个验证目标是"牵/收/断是否直觉、甩动是否有爽感、预判线是否真的帮到玩家"，都不需要敌人 AI。引入追击会带出死亡、重开、墨蚀 UI 等 M0 不需要的复杂度，稀释试玩反馈 |
| **影响面** | 与 FR-CBT-005（攻击前 0.5s 形态预告，R2）不冲突；M1 才需要真正的敌人行为 |

### D-027 · 主角与被自己牵住的物体不参与碰撞

| 项 | 内容 |
|---|---|
| **状态** | **已被 D-035 取代**（豁免范围从"被牵住的那一具"扩大到"全部 prop"） |
| **决策** | `WorldConfig.collideWithHeld = false`（默认）：主角与**当前被丝牵住的那具刚体**之间不做碰撞检测。断丝/重凝后立刻恢复碰撞 |
| **为什么** | 这是纯玩法取舍，有实测数据支撑：开启碰撞时，石块一被收近就顶在主角自己的盒体上（主角右边缘 4.40m，石块半径 0.5m ⇒ 石块卡在 x≈4.90m），**再也抬不起来**，甩动速度上限只有 **5.8 m/s**；而设计 §7 明确要求"石头被拉向主角……悬在半空，开始摆动"。关掉之后石块可以被收到手部附近并绕主角旋转，石块速度实测可达 **15–23 m/s**，`v ∝ 1/r` 的角动量加速才真正成立 |
| **代价** | ① 被牵住的物体会穿过主角的身体（视觉上要在 R2 用墨色遮挡或"手部抓握"表现掩盖）；② 主角不能站在自己正牵着的物体上 |
| **替代方案** | 若试玩后觉得"穿过身体"很出戏，可改为**缩小主角碰撞体到躯干**（0.4×0.6）并把出丝点抬到头顶，让石块能从头顶过去——但那会牺牲"石块贴着主角身侧"的其余手感 |
| **影响面** | M0 手感的核心前提之一；R2 若引入自转/更复杂形状需重新评估 |

### D-028 · 丝线刚度与阻尼的定标（修正 D-021）

| 项 | 内容 |
|---|---|
| **状态** | 已决（**数值待真机调参**） |
| **决策** | `k = 1000 N/m`、阻尼比 `ζ = 0.2` |
| **为什么 ζ 必须小** | 弹簧-阻尼绳在**突然绷紧**时的峰值张力约为 `v·√(k·m)`，阻尼项在绷紧瞬间再叠加 `2ζ√(k·m)·v`。初版取 `ζ = 0.5` 时，玩家只是"跑开把丝拉直"就会产生 ~480 N 的瞬时张力——实测 383–397 N，**足以绷断**。降到 0.2 后同样动作峰值约 356 N，留出余量 |
| **为什么 k 从 1600 降到 1000** | 峰值张力 `≈ v·√(k·m)`，k 越大越容易断。k=1000 时：静吊石块伸长 0.08m（占 6m 的 1.3%，仍是"丝"的观感），而 6 m/s 绷紧的峰值从 480 N 降到 379 N |
| **影响面** | FR-PHY-004/006 / AC-07；D-021 的常数部分作废 |

### D-029 · 张力"显示刻度"与"物理断裂载荷"分离 ★

| 项 | 内容 |
|---|---|
| **状态** | 已决（**待创始人追认**） |
| **决策** | 设计文档的 **0–400（素丝）/ 0–700（韧丝）是显示刻度**，不是牛顿。物理断裂载荷取 **1200 N（素丝）/ 2100 N（韧丝）**，两者比例 400:700 原样保留 |
| **反推依据** | 取设计文档自己的参考算例：附录 A 把"石块(4) @ 20 m/s"当作一次正常投掷，而一次自然甩动的半径约 2 m，此时向心力 `F = m·v²/r = 4 × 400 / 2 = 800 N`。要让这个正常操作留出约 30% 余量，取 `800 / 0.7 ≈ 1143 N`，圆整到 **1200 N** |
| **对照表** | 静吊石块 80 N（7%）· 跑开拉直 ~379 N（32%）· 石块 20m/s @ r=4m 400 N（33%）· 石块 20m/s @ r=2m 800 N（67%）· 石块 25m/s @ r=2m 1250 N（104% → **断弦**）· 石块 20m/s @ r=1m 1600 N（133% → **断弦**） |
| **保留下来的设计意图** | 张力四档（60% / 85% / 95%）全部按**占额定比例**计算，所以视觉与听感的分档与设计 §2.4 逐字一致，没有因为重新定标而改变 |
| **与 D-037（未来）的关系** | 若 R2 引入"韧丝"心法，只需把 `TENSION_BREAK_FORCE_TOUGH = 2100` 接上，比例关系已就位 |
| **影响面** | FR-PHY-004/006 / AC-07；**SRS 需要的不是修改，而是补一句"张力值 0–400 为显示刻度"** |

### D-030 · 收丝机在额定负载处堵转

| 项 | 内容 |
|---|---|
| **状态** | 已决（**待创始人试玩追认**） |
| **决策** | 丝上张力一旦达到 `REEL_STALL_RATIO × T_max`（0.8），收丝就**暂停缩短目标丝长**；张力回落后自动恢复 |
| **为什么必须有** | 张力来自弹簧伸长 `T = k·ΔL`，而 ΔL 由"目标丝长"与"实际长度"之差决定。若允许玩家把目标丝长一路收到最短，只要物体被挡住（顶在墙上、卡在主角身上），ΔL 就会无限增长、张力无限上升——**新手在序章"按住不放"就一定会自己把丝绷断**（实测峰值 752 N），与设计 §7 的教学节奏直接冲突 |
| **物理上的对应** | 真实绞盘在额定负载处会堵转。加上这条之后，"按住不放"最多把张力推到 80%（正好覆盖设计 §2.4 的"轻微波动"档），而**超限断弦改由动力学负载触发**——甩动的离心力、急停、被坠落物猛拽。这才是"超限断弦"的本意：它是**操作失误的惩罚**，不是**新手陷阱** |
| **副产品（意外的好处）** | 堵转同时给出了"要甩得快就必须在大半径上甩"：该张力下的速度上限 `v = √(T·r/m)`，半径 0.5m 时约 6.3 m/s、半径 5m 时约 20 m/s。这正是设计 §4.2「流星」要教的东西 |
| **影响面** | FR-PHY-002/006；由 `tests/scene_m0.test.ts` 的"纯收丝不会自己把丝绷断"锁死 |

### D-031 · 关卡尺度受投掷射程约束（M0 房间的实际后果）

| 项 | 内容 |
|---|---|
| **状态** | 已决（**R1 关卡设计的前置约束，建议写进 SRS 的关卡设计约束**） |
| **发现** | 重力 `g = 20 m/s²` 下，抛体最大水平距离是 `v²/g`。M0 实测石块可甩出 **15–23 m/s**，对应最大射程 **11–26 m**。初版 M0 房间把墨甲放在 x=20（距主角出生点 16m），导致**绝大多数甩动都够不着**——演示脚本因此完全打不中 |
| **决策** | M0 的墨甲出生点改到 x=13、往返区间 [9, 22]，交战距离始终留在射程内 |
| **给 R1 的约束** | **任何"需要用投掷解决"的遭遇战，目标距玩家的水平距离必须 ≤ 8m**（取 20m/s 射程 20m 的一半作为安全边界，为瞄准误差和抛物线留余量）。这条应该进 SRS 的关卡设计约束，否则关卡设计师会凭感觉摆放物件，做出"物理上打不到"的关卡 |
| **影响面** | FR-CBT-009（每场遭遇战 ≥3 条通路）、设计 §6.1（「门」的摆放）、R1 序章 12 分钟的关卡尺度 |

### D-032 · 张力到顶不再断丝，改为「拉紧」 ★★

| 项 | 内容 |
|---|---|
| **状态** | **已批准，SRS 已更新**（创始人于第 4 轮明确批准走 RTM 流程） |
| **原设计** | 设计 §2.4：张力超限 → **断弦**，一声"啪"，主角反噬**硬直 0.8s**；对应 FR-PHY-006（P0/R1） |
| **创始人原话** | 「丝线达到最大张力时不断裂，而是会拉紧（表现为如果物体能动且质量比主角轻则物体被拉动，否则主角无法继续往丝线张力增大的方向移动）」；理由是「在实际操作中**丝线什么时候会断太难掌握了**」 |
| **决策** | 张力到达上限时丝线**不断裂**，而是变成**刚性距离约束**：把两端钉在 `目标丝长 + 弹性余量` 上，修正量按逆质量分配 |
| **为什么这个机制更好** | ① 张力从一件**不可预测的危险**变成一条**可预测的能力边界**——玩家随时知道自己在什么状态，这正是"读懂物理"的前提；② 它让"质量差决定谁是主动方"（设计 §2.2）在极限状态下也成立，而不是被"啪"一声打断；③ 它和铁律 1「丝只能拉不能推」完全同源，没有任何特例；④ 实测甩速反而更高（12.3–22.6 m/s，旧版 12–23），因为刚性杆比弹簧更擅长传递角动量 |
| **代价** | **失去"超限断弦"这个风险机制**：丝线不再有"用坏"的可能，资源管理只剩"丝位数量 + 1.5s 重凝"。设计 §2.4 的"啪"与硬直暂时失去触发源 |
| **保留** | `World.stunRemaining` 与"硬直期间不响应输入"的机制保留（留给受击、坠地等其他硬直来源）；主动断丝（点击/右键/Q）不受影响 |
| **连带删除** | **收丝机堵转**（D-030）不再需要：它的存在理由是"防止弹簧伸长失控把丝绷断"，而伸长量现在被位置约束直接夹住。收丝因此变成纯粹的直接控制，不再有"推不动就停"的迟钝感 |
| **影响面** | **FR-PHY-006 需要改写**（从"超限断裂 + 硬直"改为"到顶变刚性"）；设计 §2.4 的断弦描述与 FR-PHY-012（抖腕冲量）需一并复核；D-030 作废 |
| **已执行（2026-09，创始人批准后）** | SRS 升 **v1.2**：改写 FR-PHY-006 / AC-07 / §1.3 术语表 / UC-02 异常流程 4a；`需求追踪矩阵.md` §6 补入本次变更的影响回溯实录；设计 §2.4 加 v1.2 修订说明 |
| **对产品的后果** | **失去"丝线会被用坏"这个风险机制**。资源管理只剩"丝位数量 + 1.5s 重凝"（FR-PHY-007）。若 R2 想补回风险，需要另设机制（例如丝位耐久），不能再靠"突然断掉" |
| **待确认** | 80%/95% 两档表现还要不要保留（现在 100% = 拉紧，而不是"快断了"）。M0 暂保留四档，`100%` 表现为"丝线绷直、最细最亮" |

### D-033 · 脱离豁免：已经在主角身体里的物体不再撞主角

| 项 | 内容 |
|---|---|
| **状态** | 已决（修复实机反馈 #4） |
| **实测** | 摆动过程中石块到主角中心的最近距离只有 **0.129 m**，而主角半宽 0.4 / 半高 0.8 ⇒ **断丝那一刻石块在主角身体内部**。豁免一撤销，求解器立刻把它猛推出去，**主角被一起撞飞** |
| **决策** | 丝线脱离时给该物体打上 `ignorePlayer`：暂时不与主角碰撞，**等两者彻底分开后自动恢复**。用包围盒判定"分开"，宁可多豁免一帧 |
| **理由** | 「已经在你身体里的东西，不该再"撞"你一次。」这不是物理妥协——真实的物理引擎也必须处理"初始穿透"，标准做法就是暂时关闭该对碰撞 |
| **影响面** | `Body.ignorePlayer` 进入状态哈希；FR-CBT-007（主角受伤）实现时要区分"豁免期内"的撞击 |

### D-034 · 输入方案：断丝改为按键/右键，丝线加粗

| 项 | 内容 |
|---|---|
| **状态** | 已决（由创始人提出，实机反馈 #3） |
| **创始人原话** | 「实际操作中我发现**丝线太细了**，通过点击丝线来断丝的方式可能操作上**太需要精细度**了」；建议「将点击丝线来断丝改成"切断"或者通过按键断丝的效果」 |
| **决策** | ① 新增 **右键 = 断最近一根**、**`Q` = 断最近一根**（此前 `Q` 根本没实现，是 FR-ACT-005 的一个缺口）；② **按住左键不动 = 收丝**（≥180ms，终于对得上设计 §7 的「按住不放」）；③ 轻点丝线仍可断（保留 FR-ACT-004 的"丝线即按钮"），但命中半径从 22px 加到 **34px**；④ 丝线线宽从 1.5–5px 加到 **4–11px**，两端加端点圆 |
| **理由** | 需求只规定了点击半径的**下限**（≥22 逻辑像素），加大不违规；断丝多给两条不依赖精度的手段，让"丝线即按钮"从**唯一手段**降级为**备选** |
| **影响面** | FR-ACT-004（触屏点击断丝）建议补上"或使用独立按键/手势"，M0 装配说明的操作表要同步更新 |

### D-035 · 主角与 prop 类物体一律不碰撞（取代 D-027）

| 项 | 内容 |
|---|---|
| **状态** | 已决（创始人于第 4 轮确认选方案 A） |
| **背景** | 实机反馈 #6「在主角没有牵上石头的时候，主角会被石头挡住去路」。实测表明**并没有真的被挡住**（3 秒走 17.69m / 理论 18.00m，只慢 1.7%）——石块被主角推着走、始终停在前面 0.89m，**观感上**像堵着 |
| **决策** | 主角与所有 `tag === 'prop'` 的动态物体（石块、陶罐等）**完全不参与碰撞**。可以走过去，投掷物也穿得过 |
| **理由** | ① prop 是"可以随手摆弄的东西"，不该成为地形；② 它与 D-027 的动机同源——被牵物体常常在主角体内，豁免范围扩大到全部 prop 之后，连"脱离豁免"（D-033）都只在敌人身上才需要，**特判更少**；③ 与 FR-PHY-003「着地等效质量 ∞」一致：主角是所有 prop 的绝对主动方 |
| **代价** | ① 主角能穿过石块，灰盒阶段会"穿模"（R2 用美术与"手部抓握"表现掩盖）；② **主角不能站在 prop 上**——若 R2 有关卡需要"踩着石块上去"，必须重新评估 |
| **与 D-027 的关系** | D-027 只豁免"当前被牵住的那一具"，**已被本条取代**。D-033（脱离豁免）保留，仍对敌人等非 prop 生效 |
| **影响面** | `需求追踪矩阵.md` §6 已补入本次变更的影响回溯实录；FR-PHY-003 / FR-CBT-* 不受影响 |

### D-036 · 位置层约束必须交替迭代（修「墨甲和石块卡在一起」）

| 项 | 内容 |
|---|---|
| **状态** | 已决（修复实机反馈 #2） |
| **实测** | 牵着石块把墨甲顶到墙上时，石块与墨甲稳定地互相嵌入 **0.236 m**（石块半径才 0.5），肉眼就是"糊在一起"。对照实验（不牵丝、石块自己滑过去）最大穿透只有 0.075 m 且收敛到 0.002 m ——**说明问题出在丝线上，不在碰撞上** |
| **根因** | 自 D-032 起丝线是一条**刚性位置约束**，每 tick 把两端精确钉在目标距离上；而碰撞分离只修正 25% 的穿透。两条都是位置约束，各解一次的话刚性那条**每次都赢** |
| **决策** | 把「丝线刚性约束 ⇄ 碰撞分离」放进**同一个交替迭代**（`POSITION_ITERATIONS = 4`），并把位置修正系数从 0.25 提到 **0.8**。迭代次数固定 ⇒ 确定性不受影响；M0 只有 7 具刚体，代价可忽略 |
| **效果（实测）** | 最大穿透 **0.236 → 0.084 m**，稳态 **0.196 → 0.037 m**（约石块半径的 7%，肉眼不可见） |
| **教训** | 只要引入了**第二个位置约束**，"各自解一次"这个隐含假设就不成立了。R2 引入更多约束（关节、缠绕）时必须回到这里检查迭代结构 |

### D-037 · 攻击门槛：「断丝 = 攻击」★

| 项 | 内容 |
|---|---|
| **状态** | 已决（创始人实机反馈 #3，方案由我提出） |
| **创始人原话** | 「我发现你做出来的机制是**只要石头碰到了敌人就会造成伤害**，这样就会产生一个问题：**我可以直接把石头拉过去就能击杀敌人**，这和我原来通过甩过去砸到敌人击杀的初衷不符」 |
| **决策（两道门槛）** | ① **被丝牵住的物体不造成伤害**——设计 §2.3 原文就是「**断丝 = 攻击**」，攻击动作本身就是"松手"；② **最小伤害速度 `MIN_DAMAGE_SPEED = 6 m/s`**，把"蹭到"与"砸到"分开（6 = 主角自己的走速） |
| **为什么门槛 ① 是正解而不是补丁** | 它不是"加一条限制"，而是把设计文档里**已经写明**的规则落到代码：攻击 = 断丝。同时它自动消灭了一整类退化玩法（把石头收过去磨、把敌人顶到墙上蹭、用丝把敌人挤死） |
| **与 `MIN_IMPACT_SPEED` 的分工** | 0.5 管"有没有接触反馈（音效/表现）"，6 管"算不算伤害"。轻轻碰到仍会有撞击反馈，但不会掉血 |
| **不影响的** | 环境击杀（`无丝` 命名，物体从未被牵过）仍然成立；`借刀`（用敌人的武器）仍然成立——那些物体本来就不在你手上 |
| **影响面** | FR-CBT-002 / AC-02；**建议在 SRS 的 §2.5 伤害公式处补一句"被丝线牵住的物体不造成伤害"** |

### D-038 · 敌人不可被丝线附着（R1 阶段）

| 项 | 内容 |
|---|---|
| **状态** | 已决（创始人实机反馈 #1：「丝线能牵到敌人，还能把敌人拉动，这个设计是合理的吗？」） |
| **依据（设计文档原文，不是我新定的）** | 设计 §5 心法表：**「墨丝」（第四章中）—— 可以附着于「无相」本体 → 打开的技术空间：甩动敌人打敌人（双截棍）**。也就是说"牵敌人"**是第四章才解锁的能力**；在此之前敌人不是可附着目标 |
| **结论** | **不合理，是我实现错了**。`M0Scenario` 里墨甲的 `anchorable` 误设为 `true`，已改为 `false` |
| **为什么这个错误很危险** | 它不只是"多了一个能力"——"牵敌人"直接跳过了「双截棍」这个设计好的技术空间（§4.2 留给玩家自己发现的第 5 条），而且让"甩动敌人打敌人"提前变得廉价 |
| **R1 之后怎么接** | `Body.anchorable` 已经是逐刚体字段，接「墨丝」心法时只需按心法解锁把它打开（例如 `anchorable = hasMohistSkill`）。不需要改结构 |
| **影响面** | FR-PHY-013（只能附着于 `Anchorable`）/ FR-PRG-002（8 种心法）；`scene_m0.test.ts` 已有回归断言 |

### D-039 · 道具摩擦降到 0.1（补偿 M0 没有自转）

| 项 | 内容 |
|---|---|
| **状态** | 已决（修复实机反馈：「石头速度衰减太快，导致我基本上要贴近敌人甩出去才能砸到」） |
| **实测（石块 20 m/s 平抛）** | 摩擦 0.4（旧）⇒ 等效 μ=0.566 ⇒ 减速度 **11.3 m/s²** ⇒ 12m 处只剩 11.3 m/s（伤害 11.3）<br>摩擦 **0.1（现）** ⇒ μ=0.283 ⇒ 5.7 m/s² ⇒ 12m 处 **16.2 m/s**（伤害 **16.2**） |
| **根因** | **M0 没有刚体自转（D-025）⇒ 石块不能滚，只能滑。** 滑动摩擦远大于滚动摩擦，于是 20 m/s 投出去在 9m 处只剩 14、12m 处只剩 11、15m 处只剩 7.7（逼近伤害门槛 6）。有效满伤害射程只有 ~9m |
| **决策** | 新增 `PROP_FRICTION = 0.1`，用于所有可投掷道具（石块、陶罐……）。这是**等效摩擦**：用低滑动摩擦近似"它会滚" |
| **为什么不取更小** | 真实滚动摩擦在 0.01–0.05 量级，但视觉与手感上太"冰"（石块要滑 50m 才停）。0.1 是折中：12m 处保留 **81%** 速度（旧值 57%），有效满伤害射程 **~9m → ~15m**，石块仍在 3 秒左右自然停下 |
| **最强的证据** | **内置演示脚本自己变了**：修复前需要助跑 180 tick（3 秒）才能砸到墨甲，修复后只需 **30 tick（0.5 秒）**；断丝时机从第 397 tick 提前到第 **121** tick |
| **与 D-025 的关系** | 这是 **D-025 的补偿措施**。R2 若引入刚体自转，`PROP_FRICTION` 应改回真实滑动摩擦值，让"滚"本身产生低阻力——那才是物理上正确的做法 |
| **影响面** | FR-CBT-002 / AC-02；`physics.test.ts` 新增"投掷射程回归护栏"（12m 处必须 > 15 m/s）；**建议给 SRS 补一条关卡约束："投掷有效射程 ≥ 12m"**，与 D-031 的"交战距离 ≤ 8m"一起把关卡尺度夹住 |

### D-040 · 主角受伤：有 HP，但表现是墨蚀、绝不显示血条

| 项 | 内容 |
|---|---|
| **状态** | 已决（创始人提问后确认） |
| **决策** | ① 主角**有隐藏 HP**（有受伤、有死亡）；② **绝不显示血条**；③ 受伤表现 = **墨色从画面边缘向内侵蚀，抵达中心判定死亡**；④ **R1 不实现**主角承伤与死亡机制——**序章的墨卒不伤人** |
| **「不显示血条」的文档依据（6 处一致）** | 设计 §8.4「**没有血条。** 主角受伤用画面表现：墨色……」· 视觉规范 §219 同· 视觉规范 §279 禁止项 7「❌ 添加血条、伤害数字、技能图标」· 视觉规范 §304· SRS §1.3 术语表「**墨蚀** ｜ 画面边缘向内侵蚀的受伤表现，**替代血条**」· FR-UI-005（P0/R1）「不显示血条」 |
| **HP 是隐藏数值的证据** | FR-OPT-003（R3）的辅助模式含「**墨蚀宽容**」——纯表现是没法"宽容"的，背后必然有一个可调的数值量 |
| **墨蚀方向的矛盾与裁定** | 设计 §8.4 写"墨色**向画面边缘**侵蚀，到边缘 = 死亡"，而**视觉规范 §219、FR-CBT-007、AC-25 三处**都写"**从画面边缘向内**侵蚀，到中心 = 死亡"。**采纳后者（4:1）**，理由：<br>① 契合「大量留白」的美术方向——墨蚀吃掉的是留白，主题上更贴；<br>② 不遮挡 L4 玩法层中心（玩家与敌人在那里），玩家永远看得见自己在干什么；<br>③ 暗角收拢是玩家**一眼读懂**的"我快死了"，不需要学习 |
| **为什么 R1 不做死亡** | AC-01 测的是「**玩家会不会自己想到用石块**」。若序章会死，同一场测试就同时测了两件事（会不会用石块 + 会不会死），**核心指标被污染**。且 FR-CBT-007 的发布阶段本来就是 **R2**。设计 §7 的序章文本也从未写墨卒会攻击玩家 |
| **已执行** | 设计 §8.4 加 v1.2 修订说明，把措辞统一为"从画面边缘向内侵蚀，侵蚀到中心 = 死亡" |
| **⚠️ 仍待定（R2 实现 FR-CBT-007 前必须定）** | ① 主角 **HP 数值**；② 墨蚀比例 ↔ HP 的**映射**；③ **敌人对主角的伤害公式**（附录 A 只定义了玩家打敌人，附录 B 只有敌人血量）——**这三项文档里一个字都没有** |

### D-041 · 关卡完成判定：穿过「门」即过关，敌人不必清空

| 项 | 内容 |
|---|---|
| **状态** | 已决（创始人于 M1 开工前确认） |
| **背景** | SRS 里**没有任何一条需求定义"关卡完成条件"**：FR-LVL-003 只规定了"每关设一道「门」作为进度锁"，没说"通过门即过关"；也没有需求定义"关底/BOSS 被击败 → 章节完成"。**这是 M1 的硬阻塞**——序章那道深沟就是门 |
| **决策** | **关卡完成条件 = 穿过本关的「门」到达另一侧；敌人不必清空。** 章节完成 = 击败关底 / BOSS |
| **序章的具体表现** | 8:00–12:00 那道**深沟**就是门：荡过去（连横梁）或用双丝、或任何别的办法到达另一侧 = 通关 |
| **理由** | 与设计 §6.1 完全一致——「『门』是唯一的教学手段」「有 ≥3 条通向胜利的物理路径」。若要求"清空敌人"，就与「**无丝**」命名（单场战斗全程未使用丝线，纯环境击杀）直接冲突，也会把双丝的多解空间压成单解 |
| **必须补的 SRS 条目（待走 RTM）** | 新增 FR：「**关卡完成条件 = 穿过本关的「门」；敌人不必清空**」；并补"关底/BOSS 被击败 → 章节完成" |

### D-042 · AC-01 埋点口径：完整时间线 + 可一键导出 ★

| 项 | 内容 |
|---|---|
| **状态** | 已决（创始人于 M1 开工前确认） |
| **决策** | 本地逐事件 JSONL 记录：**时间戳 / 位置 / 操作 / 连接 / 断丝 / 击杀 / 用丝数**，并提供**一键导出**（M1 先导出 JSONL，后续可加 CSV） |
| **为什么必须记这么细** | AC-01 是**项目存续的判定点**，交接清单 §5.2 对测试的要求是「**不要给任何提示，不要在旁边看你操作**」。若靠人工拄表，记录者必须在场盯着——**这本身就破坏了测试条件**。只有自动埋点能做到"测完拿到数据"。而且"60 秒内自主使用石块击杀"这个判定可以**逐帧复核**，不靠人眼 |
| **隐私面** | 只记游戏内事件（位置/操作/时间），不记任何个人信息、不联网上传。文件由一个测试者产生一个，由创始人手动取回 |
| **与确定性的关系** | 埋点必须**不引入不确定性**（不能用 wall-clock 影响模拟）；时间戳只在写盘时附加，不参与 `stateHash` |
| **影响面** | FR-LIV-003（埋点）/ AC-01 / NFR-MNT-003（回放）；数据契约建议一并写进 SRS 的 §7 数据需求（DR-*） |

### D-043 · M1 分批交付计划（按设计 §7 的时间段）

| 项 | 内容 |
|---|---|
| **状态** | 已决（创始人确认） |
| **决策** | M1 **不是一次性做完 12 分钟再交**，而是按设计 §7 的时间段切成 4 批，**每批都能跑、能玩、有回归测试** |
| **批次** | **批 1（0:00–3:00）**：序章场景骨架 + 牵/收/断 + **陶罐**（可破坏场景物）+ 极简提示系统<br>**批 2（3:00–5:00）**：**墨卒**（FR-CBT-001 的第 2 类敌人）+ 60 秒无动作的克制提示 + **埋点（D-042）**<br>**批 3（5:00–8:00）**：质量差（60 质量梁柱 + 0.6 质量陶罐）<br>**批 4（8:00–12:00）**：悬吊横梁 + 深沟（= 门，D-041）+ **第二根丝解锁** + 过关判定 |
| **理由** | M0 已经证明这个节奏有效——创始人每一轮都能当场发现真问题（6 条 + 3 条反馈里有 4 条是我的实现 bug）。一次做完 12 分钟再交，等于把四个批次的风险堆到最后一次性引爆 |
| **连带发现的 SRS 缺口（待补）** | **FR-PRG-006（丝线上限 1→2→3→4）目前标的是 R3**，但设计 §3.3 与 §7 的 8:00–12:00 都要求**序章内就解锁第二根丝**。这条的发布阶段需要从 R3 改到 R1，否则批 4 无据可依 |

### D-044 · 可破坏场景物走与敌人相同的伤害管线

| 项 | 内容 |
|---|---|
| **状态** | 已决（批 1 实现，设计 §7 2:00–3:00「石头飞出，砸碎一个陶罐」） |
| **决策** | ① `resolveDamage` 的目标判定从 `tag === 'enemy'` **泛化为"任何 `maxHp > 0` 的活体"**；② 陶罐 = `hp: 1` + `weakness: 'any'` + `shattersOnDeath: true` |
| **为什么不需要为陶罐单开一条规则** | 现有两条公式恰好就表达了"必须以投掷速度砸上去才会碎"：<br>· **冲击**要求 `m_eff ≥ 3`，而 `min(石块 4, 陶罐 0.6) = 0.6` **被门槛挡下**<br>· **切割** `v²/60` 在 `v ≈ 7.8 m/s` 时才够 1 点伤害<br>实测：3 m/s 蹭上去**不碎**，20 m/s 砸上去**碎** —— 与设计 §7 的教学意图完全一致 |
| **新增两个刚体字段** | `shattersOnDeath`（死亡即碎裂消失，区别于敌人留在场上）· `removed`（已移出世界） |
| **⚠️ 为什么不从 `bodies` 数组里删元素** | **数组下标就是刚体 id**，一删所有 id 错位、求解器顺序改变，**确定性直接崩掉**（AC-05）。所以只打 `removed` 标记，碰撞检测与渲染各自跳过它 |
| **影响面** | FR-CBT-002/003（伤害管线）/ FR-LVL-005（每关 ≥3 种可交互环境物体，R2）；`tests/prologue.test.ts` 有回归断言 |

### D-045 · 序章用「进度门控」而不是「时间轴」（**待创始人确认**）

| 项 | 内容 |
|---|---|
| **状态** | 已实现，**待创始人试玩追认** |
| **背景** | 设计 §7 用**时间轴**写序章：「0:00–1:00 牵……3:00–5:00 第一个墨卒」。但同一节又写着「**连上后**丝线绷紧……**提示消失**」——说明它更在意"玩家做到了"而不是"秒表到了" |
| **决策** | 提示的推进、以及**遭遇战的开始**，都由**玩家进度**门控：把牵/收/断三件事都做过（`hintStep === 'done'`）之后，再等 1 秒，墨卒入场 |
| **理由** | 手慢的玩家不该在还没学会"牵"的时候就面对敌人——那会让 **AC-01 测出的不是"会不会自己想"，而是"看不看得懂"**。AC-01 的计时起点本来就是"遭遇战开始"，不是"游戏开始"，所以这个改动**不改变判定口径** |
| **代价** | 与设计文档写的时间轴不再逐秒对应。若创始人认为"12 分钟必须是固定的 12 分钟"（例如为埋点对齐、或为剪辑宣传片），改回计时即可——`PrologueScene.advanceStage()` 是唯一要动的地方 |
| **影响面** | 设计 §7 的执行方式；AC-01 的计时起点（不变） |

### D-046 · `damage` / `killed` 事件补 `by` 字段

| 项 | 内容 |
|---|---|
| **状态** | 已决（批 2 实现，为 AC-01 埋点服务） |
| **决策** | 内核的 `damage` 与 `killed` 事件增加 `by: number`（攻击者刚体 id） |
| **理由** | AC-01 的判定是「**60 秒内自主使用石块击杀**」——埋点必须知道"是**谁**砸死的"。原先事件只带目标，埋点只能靠"离受害者最近的刚体"这种启发式猜，出现"两块石头同时命中"就会歧义。**让内核直接说出凶器，比在下游猜要硬得多** |
| **影响面** | `core/events.ts` / `core/world.ts` / `core/telemetry.ts`；事件是新字段，老消费方不受影响 |

### D-047 · 梁柱是"不可动的结构"，不是"质量 60 的动态刚体" ★

| 项 | 内容 |
|---|---|
| **状态** | 已决（批 3 实现）；**下方那处设计文档冲突需要创始人裁定** |
| **冲突原文** | **设计 §7**（5:00–8:00）：「玩家连梁柱 → 收丝 → **自己被拉了过去**」<br>**设计 §2.2**（玩家状态机）：「着地 → 等效质量 = ∞ → 拉丝时：**物体被拉向你**」<br>两者**不可能同时成立**。而 D-019 已把着地质量定为 1e6，那么 60 质量的梁柱会被玩家拖过来——与 §7 正好相反 |
| **裁定** | 取 **§2.6 自己的定性**：「梁柱 / 佛像部件 40–60 ｜ **超重，主要作为锚点与移动手段**」。它是**锚点**，所以做成 `static`。名义质量 60 保留在 `damageMass` 上（伤害计算与可读性用） |
| **为什么这样做是对的** | ① 不可动的锚点让"你被拉过去"自然成立（刚性约束把玩家拉向锚点，**与质量无关**）；② 它也顺手给了"离地"一个起点——设计 §4.2 的摆荡明确要求"**离地状态**"，而着地质量 ∞ 的玩家本来永远无法被丝拽离地面；③ 不需要改动任何已批准的物理决策 |
| **形态选择** | 做成**横跨房间上方的横梁**（x 10..26，y≈13）而不是竖在地上的柱子。理由：竖柱会挡住墨卒的行走路线与投掷弹道；横梁在头顶时，从下方收丝的拉力有强烈向上分量，**玩家会被直接拽离地面**——这是"重的东西用来移动"最直白的一次演示 |
| **待创始人裁定** | 若你认为"质量差"必须是**数值意义上的**（60 vs 0.6，两个都是动态刚体），那就需要把 D-019 的着地质量从 1e6 降到一个有限值（例如 50），让"玩家被 60 质量的梁柱拉走"在数值上成立。**代价是所有手感都要重新标定**（M0 的验收结果会失效） |

### D-048 · 附着与丝线锚点按「表面最近点」算（大物体必需）

| 项 | 内容 |
|---|---|
| **状态** | 已决（批 3 实现） |
| **问题** | `pickAnchorable` 原先比较的是**瞄准点到刚体中心**的距离。序章的横梁有 **16 米宽**——玩家瞄准它的左端时，离它中心的距离是 8 米，**远超附着容差 1.2 米，根本连不上**。而且就算连上了，丝线锚点取中心会让绳长瞬间变成 16.8m（上限 12m），**一连接就被猛拽** |
| **决策** | ① **选择**：用"瞄准点到物体**表面**的距离"（在内部为 0）；② **锚点**：`targetAnchorOf()` 取"离主角最近的表面点" |
| **但小物体仍取中心** | 判据是"半尺寸是否超过附着容差"：超过才用表面点。<br>**为什么不能让所有物体都用表面点**：实测把石块的锚点从中心挪到表面（差 0.5m）之后，有效绳长变短，甩速从 **17 → 12.9 m/s**，把**已通过 M0 验收的手感改坏了**。小物体取中心是标准模型，且与既有标定一致 |
| **影响面** | FR-PHY-013（附着约束）/ FR-ACT-009；新增 `closestPointOnShape()` / `distanceToShape()`（纯函数，可单测）；对后续所有大型锚点（佛像部件、梁柱、悬吊横梁）都必需 |

### D-049 · 序章 8:00–12:00 的过沟路线：**两步被拉过去**，不是荡过去 ★

| 项 | 内容 |
|---|---|
| **状态** | 已决（批 4 实现）；**一处待创始人试玩确认**（见末行） |
| **问题** | 设计 §7 只说「双丝 + 一根悬吊的横梁 + 一道深沟」，**没有说怎么过**。深沟 6 米宽、主角没有跳跃，所以必须用丝；但"用丝"有很多种几何，得先算清楚哪一种真的走得通 |
| **先算了一遍"荡过去"，结论是走不通** | 从沟左沿（14.6, 1.0）被丝拉住、以静止姿态起摆，摆到对岸同高度时的水平位移最大就是 2×(支点x − 14.6)。锚点取的是**离主角最近的表面点**（D-048），所以支点不是梁心而是**梁的左端**；想让摆幅够到对岸平台（x ≥ 21.4），梁的左端得落在 x ≥ 19 —— 而那样人又会撞上平台边角。**这条路线在数值上极其边缘，不能作为"唯一解法"** |
| **改为"被拉过去"（采用）** | 全程用**已有的收丝直接控制** + **已有的质量差知识**，不依赖摆荡的手感：<br>**①** 站沟左沿连**悬吊横梁**（18, 9.5）→ 收丝 → 沿半径被收进去，稳稳悬在**沟正上方**（18, ≈8.4）；<br>**②** 连**对岸吊桩**（28, 11）→ **断掉第一根**（不断的话第一根会把人拽回沟心）→ 收丝 → 被拉到对岸上空 → 断丝 → 落到平台 |
| **为什么"两步"是必须的** | 从沟左沿到对岸吊桩是 **16.7m > 丝长上限 12m**，一步够不着 → **深沟是一道真门**（回归测试断言 `targetId === -1`）。而从悬吊横梁下方到吊桩只有 **10.3m** → 够得着。所以"先上悬吊横梁"不是可选的花样，**是唯一入口** |
| **第二根丝在这里到底需不需要** | **需要，但不是数学上的硬性要求**：第二根丝的价值是让你可以"**先连上、再断旧的**"，全程没有自由落体的计时压力。理论上可以断掉第一根之后在半空中抢连吊桩（落体窗口约 0.6 秒），那是高手路线。**这一条留给实机试玩判断**：如果玩家从头到尾没碰过第二根丝，就说明"双丝"这一段没教到东西，需要把吊桩再放远/抬高，把窗口关掉 |
| **顺带发现：主横梁曾经是一根"滑轨"** | 横梁的锚点会**跟着人横向滑动**（D-048 的副作用），所以它天然是一条轨道。批 4 最初把主横梁做成 x 10..26（横跨深沟）时，玩家可以连上它、一路滑到对岸上空、松手落到平台——**一根丝就通关，悬吊横梁与吊桩双双成了摆设**。已把主横梁收到 **x 4..14**（沟左沿是 15），它只能把人送到沟沿 |
| **待创始人试玩确认** | ① 上面那个"第二根丝是否必需"的问题；② 8:00–12:00 全程**零提示**，玩家要自己把"牵→收→断"和"重的东西把你拉过去"串成两步链——**这个难度是否合适**（太简单＝没有"门"，太难＝卡住）。两处都只改 `constants.ts` 里的 `FAR_POST_*` / `SWING_BEAM_*` 四个数 |
| **影响面** | 设计 §7（8:00–12:00 的执行方式）、§6.1（"门"的定义）；新增常量 `FAR_POST_*` / `CHASM_CLEAR_MAX_Y`；`tests/prologue.test.ts` 有"可通关 + 走不过去"双向回归 |

### D-050 · 静态结构**永远**与主角碰撞；深穿透不算"站得住"

| 项 | 内容 |
|---|---|
| **状态** | 已决（批 4 实现）——**修的是两个真 bug，不是风格问题** |
| **背景** | `detectContacts()` 原先对"**正被丝牵住的物体**"一律不与主角碰撞（D-027 的残留，本意是让石块别顶在主角身上，见 D-035）。但**静态结构**也被这条豁免了 |
| **Bug ①：收丝会把人拉进横梁里** | 主角连上主横梁、一直收丝 → 横梁不动、主角穿过去 → 丝线锚点是"离主角最近的表面点"，手一旦进入横梁内部，锚点就变成手自己、**绳长塌成 0**、位置约束**瞬间消失** → 人继续被重力拉下、再被约束拉起……实测在梁里以 **vy ±25 m/s 永久抖动**，截图上看就是"卡在梁里抽搐" |
| **修法** | 豁免只给**可动物体**（`other.kind !== 'static'`）。静态目标本来就拉不动，豁免它**没有任何收益**；不豁免则主角老老实实停在横梁下沿（实测稳定在 y=11.497，vy=0，剩余张力 10N） |
| **Bug ②：嵌在横梁里被判成"站在对岸"→ 误通关** | 被卡住时，接触法线常常是"从人指向脚下的结构"（人踩在悬吊横梁顶面、身体嵌在主横梁底面），`updateGrounded()` 于是判 `grounded = true`，而 `checkCleared()` 只看"着地 + x 过了沟" → **玩家在 12 米高处被判通关** |
| **修法（两层）** | ① **判定层**：`GROUNDED_MAX_PENETRATION = 0.2` —— 正常接触修正后穿透在 0.01 以下，能到 0.2 的只有"被挤住、求解器推不出来"这一种情况，它不该算立足点；② **判定层**：过关额外要求 `y ≤ CHASM_CLEAR_MAX_Y = 1.5` —— "站在对岸**平台上**"和"站在沟上方的梁上"是两件事 |
| **几何层也顺手修了** | 悬吊横梁原先在 y=11（顶面 11.35），到主横梁底面（12.4）只有 **1.05m**，而主角高 **1.6m** —— 这个缝本来就塞不进去。已把悬吊横梁降到 **y=9.5**，缝变成 2.55m。**教训：灰盒关卡的竖向间距必须大于主角身高**，否则一定会出现"挤在中间"的状态 |
| **影响面** | FR-PHY-004/005/007（碰撞与着地）、FR-PHY-013；`core/world.ts` 两处；新增常量 `GROUNDED_MAX_PENETRATION`；回归 `批 4：收丝贴到静态结构上是停下来，不是陷进去抖` / `站在高处的结构上不算过关` |

### D-051 · 序章第一个墨卒改从**左平台内侧**入场（x=12）

| 项 | 内容 |
|---|---|
| **状态** | 已决（批 4 实现） |
| **背景** | 深沟（批 4）把房间切成两半，8:00 之前的一切都必须发生在**左平台（x < 15）**上。而墨卒仍按 M0 的 `MOTE_SPAWN_X = 30` 入场 |
| **Bug ①：敌人自己走进沟里** | 它只盯主角走。玩家跑到平台右端，它就跟着越过沟沿、掉进沟底、再从沟底走出去、**永远下落**（实测 y 一路到 −2215 还在涨，状态哈希迟早变 NaN）。表现是"3:15 之后这一关没有敌人了"，**AC-01 永远不可能达成**。修法：给它一个序章专用入场点，并加**沟沿护栏**（`PROLOGUE_MOTE_LEDGE_GUARD_X`）——脚本驱动的东西不该自杀 |
| **Bug ②：陶罐挡了弹道** | 入场点如果放在 14（平台右端），它会一边往主角这边爬一边**推着陶罐（x=13）走**；更关键的是"石块 → 墨卒"的弹道**正好被陶罐挡掉**——石块先把陶罐打碎，墨卒毫发无伤，**AC-01 直接测不出来**（实测就是这样失败的） |
| **决策** | 序章墨卒入场点 = **12**：在主角（x=4）右侧、"从右侧缓慢爬来"依然成立；又在陶罐**内侧**，弹道干净；并且站在左平台之内。护栏 x = 14.5 |
| **代价** | 入场距离从 26m 缩到 8m，"从远处慢慢爬来"的**压迫感变弱**。R1 的这段重点是"会不会自己想"，不是压迫感；R2 若要把距离拉回来，只能靠**把沟改窄/把平台加长**，不能再让敌人走沟沿 |
| **顺带修的** | 世界地板（`pit-floor`）原先只铺沟底（15..21），任何物体只要在 y=−12 以下、又落在 x<15 或 x>21 就是虚空 → 永远下落。**已铺满整个房间**，从根上取消"世界之外" |
| **影响面** | 设计 §7（3:00–5:00 的入场）；AC-01 的**可达性**（不是判定口径）；新增常量 `PROLOGUE_MOTE_SPAWN_X` / `PROLOGUE_MOTE_LEDGE_GUARD_X`；回归 `批 2：三件事都学会后，墨卒从右侧入场`（断言已改为"在主角右侧 + 在左平台内 + 在陶罐内侧"） |

### D-052 · 丝线：**按住瞄准、松手发射，松手点即附着点**，中间要看得见飞过去 ★

| 项 | 内容 |
|---|---|
| **状态** | 已决（第 13 轮实机反馈，创始人原话驱动） |
| **创始人原话** | ①「是否可以做成玩家长按屏幕瞄准，松手后发射丝线，**松手的位置即是丝线附着的位置**（如果松手的地方是空白那么不附着）」<br>②「现在你做出来的丝线无论发射到物体的哪个位置，最终呈现出来都是附着在了物体的中间，我觉得这个不太好，**有时候我可能想把丝线射到横梁的边上**」<br>③「我想看到发射丝线从角色射到物体上的**一个动态过程**，而不是瞬间出现一条相连的线」 |
| **状态机** | `idle → flying → attached`。松手那一帧锁定「哪具刚体 + 表面上的哪一点」，丝以 `ROPE_LAUNCH_SPEED = 40 m/s` 飞过去；**飞行期间不参与施力与约束**（`applyForces` / `solveRopeConstraints` 都只看 `attached`）；到位那一帧才建 Verlet 链、才可能受力 |
| **附着点 = 松手点（字面意义）** | 松手点在轮廓内或轮廓上 ⇒ **就是那一点，不做任何吸附**；落在轮廓外 `attachTolerance`（1.2m）之内 ⇒ 吸附到最近的表面点（手指容错）；再远 ⇒ **不附着**，且**不占用丝位**（新手乱试不该有惩罚），发一个 `rope-missed` 事件供表现与埋点用 |
| **⚠️ 试过但否决的做法** | "点在物体内部就推到最近的那条边"（丝永远落在可见轮廓上，更好看）。**实测否决**：石块的锚点从中心挪到表面差 0.5m ⇒ 有效绳长变短 ⇒ 甩速从 **≥15 掉到 14.98 m/s**，正好跌破设计附录 A 的投石量级。而创始人要的"瞄横梁边上就挂边上"**本来就已经满足**（边缘在轮廓上 ⇒ 原样使用）。见 `项目进度.md` §5 第 14 项：若试玩觉得"丝插进石头里"很怪，可以换回来、并同时下调验收线 |
| **锚点锁存（意外收获）** | 附着点存成**相对目标的局部偏移** `anchorOffset`，之后跟着刚体走。原来的 `targetAnchorOf()`（"离主角最近的表面点"）每次求解都重算 ⇒ 锚点会**随人滑动**，那是 D-048 的副作用，也是 D-049 里"主横梁是一条可以吊着人横向滑动的轨道"这个漏洞的**根源**。现在从根上关掉了：挂在横梁的哪一点，就一直是那一点 |
| **落点预览** | 新增 `World.aimPreview(player, aim)`：瞄准时就能知道"能不能连、会落在哪"。渲染层据此**高亮目标物体 + 在吸附点画绿环 + 落空画红叉 + 画射程圈**。理由：「松手在空白处不附着」这条规则必须**按下去就能看见**，而不是松手才知道 |
| **代价** | ① 丝线不再是"瞬间成形"，所有"连上"的时序都往后推迟了一个飞行时间（`demo.ts` 的搜索器自己会适应，因为搜索与运行用同一套规则）；② 埋点只记 `rope-attached`（到位），不记 `rope-launched`——否则"射了一次但没连上"会被算成一次成功操作 |
| **影响面** | FR-ACT-002（牵的交互）/ FR-PHY-013（附着）/ FR-PHY-014；`core/input.ts`、`core/rope.ts`、`core/world.ts`、`core/collide.ts`；**D-048 的"锚点取最近表面点"被本决策取代**（选择逻辑仍在，锚点语义变了）；回归 `tests/silk.test.ts` |

### D-053 · 操作方案：**左右两个按钮 + 五个屏幕按键**，取消左半屏虚拟摇杆

| 项 | 内容 |
|---|---|
| **状态** | 已决（第 13 轮实机反馈，创始人原话驱动） |
| **创始人原话** | ①「左半屏幕负责移动，但是实际玩起来的时候有时候丝线需要连到左半屏幕的部分，操作起来很不方便，**既然移动方向只有左右，那就使用左右两个按键进行移动就好，没有必要虚拟摇杆占据半个屏幕**」<br>②「在角色运动中点击丝线来切断这个方式依旧很不灵敏，**是否能改为通过按键切断**？」 |
| **新方案** | 触屏：左下 **◀ ▶** 移动 + 右下 **「收」「放」「断」**；**屏幕其余部分整块都是瞄准区**（这正是取消摇杆换来的东西）。<br>键鼠：`A`/`D`（或 `←`/`→`）移动 · **按住左键瞄准、松手发射** · 滚轮上/下收放（空格/Shift 亦可）· `Q` / 右键断 |
| **收放的选择** | 创始人指定「触屏用两个按钮收放，**键鼠使用鼠标滚轮**」。旧方案里的"按住不放 = 收丝"**已删除**——它与新的"按住 = 瞄准"直接冲突 |
| **切断的选择** | 创始人选了「**保留点丝线切断，只额外加按键**」。所以两条路都在，用**时长**分开：按下到松手 < 200ms **且**松手点落在某根已附着的丝上 ⇒ 断那一根；否则 ⇒ 发射。已知副作用见 `项目进度.md` §6 缺陷 #18 |
| **单一几何来源** | 按钮矩形定义在 `core/hud.ts` 的 `HUD_BUTTONS`，**输入层用 `buttonAt()` 命中、渲染层用同一份画**。两处各写一份坐标早晚会错位，而错位的表现是"按钮看着能按、实际按不到"——这种 bug 靠读代码抓不住。`tests/hud.test.ts` 守着"不重叠、不出界、屏幕中央必须留给瞄准" |
| **影响面** | **FR-ACT-001 / FR-ACT-002 / FR-ACT-003 三条需求文本需要改写**（见 `项目进度.md` §5 第 13 项，待创始人追认）；FR-ACT-004 的主手段从"点丝线"变成"按键"；`cocos/PlayerInput.ts` 整层重写；新增 `core/hud.ts` |

### D-054 · 段落推进改为**纯进度门控**：删掉两条时间轴兜底

| 项 | 内容 |
|---|---|
| **状态** | 已决（第 13 轮实机反馈：「**无需再有时间线，玩家完成上一个操作就进入下一个场景**」） |
| **删掉了什么** | `ENCOUNTER_MAX_SEC = 120`（遭遇战到点推进）与 `MASSDIFF_MAX_SEC = 180`（质量差到点推进）。现在每一段的出口**只有玩家的动作**：教学（牵/收/断都做过）→ **打死墨卒** → 质量差（**梁柱与陶罐各连过一次**）→ 双丝 → **站上对岸平台** |
| **⚠️ 代价（必须说清楚）** | 一个在遭遇战里想不到用石头的玩家会**永远停在那一分钟**。这是**刻意的**：设计 §7 原话「这是整个游戏最重要的一分钟。玩家必须自己把前三个动作串起来」——**AC-01 要量的正是这个**，给它兜底等于把被测的东西拿掉 |
| **那怎么人工检查后面的内容** | 新增 `PrologueScene.skipTo(stage)` + `PlayableScene` 上的可选 `debugStages()/debugSkip()`，接到 `cocos/Bootstrap.ts` 的**数字键 1..5**（1 教学 / 2 遭遇战 / 3 质量差 / 4 双丝 / 5 通关）。跳段会**补齐该段应有的前置状态**（陶罐给出、第二根丝解锁、对岸墨卒入场），所以从任一段都能正常玩下去。**玩家路径上没有任何东西会调用它** |
| **保留的"计时"** | 60 秒的**克制提示**（石头微微发光）保留——它不是段落推进，是设计 §7 明写的兜底提示；段与段之间的 1 秒停顿（`ENCOUNTER_DELAY_TICKS`）也保留，那是节奏不是门控 |
| **与 D-045 的关系** | D-045 已经把"教学 → 遭遇战"改成进度门控；本决策把剩下两段也统一了，于是**整条链上不再有任何时间轴** |
| **影响面** | FR-PRG-004（序章分段流程）；`core/constants.ts` 删两个常量；`core/scene_prologue.ts` 的 `advanceFrom*`；回归「没打死墨卒干等 5 分钟仍停在遭遇战」「只摸一个对照物不推进」 |

### D-055 · 取景下移 + HUD 坐标口径修正 + 物体可读性（**两个"看不见"的 bug**）

| 项 | 内容 |
|---|---|
| **状态** | 已决（第 13 轮实机反馈：「我看不到沟在哪里梁柱在哪里，也分不清哪个是石头哪个是罐头哪个是敌人」） |
| **Bug ①：整个深沟在屏幕之外** | 取景窗口左下角一直钉在世界原点 (0,0)，而**地面顶面就在 y = 0**：可玩带贴在屏幕最下沿，`y < 0` 的一切（整道深沟）被切在屏幕外。**不是看不清，是屏幕上根本没有。**<br>修：`VIEW_ORIGIN_Y = −4.4` ⇒ 可见 y ∈ [−4.4, 13.6]。副作用正好：沟底落在屏幕外，读起来就是"掉下去就没了"，与"深沟"语义一致 |
| **Bug ②：HUD 一直被画在屏幕外** | `Graphics` 画在**节点局部坐标**（画布中心为原点，y ∈ [−540, 540]），而丝位圆点与张力条一直用 **UI 坐标**（左下角为原点）当局部坐标用 ⇒ 算出来的 y ≈ 1035 落在屏幕上方之外。**玩家从来没有见过那四枚圆点。**<br>修：新增 `uiToLocal()`，HUD 统一走它 |
| **可读性（形状 + 颜色 + 小字）** | · **深沟**：纯黑（比背景更黑）+ 两侧崖壁亮边 + 三条渐隐深度线；沟的位置**从地形量出来**（两块 `ground` 之间的空档），不硬编码关卡坐标 ⇒ M0 沙盒不会画出多余的沟<br>· **可附着的结构**（梁柱 / 悬吊横梁 / 吊桩）单独配色 + 亮边；悬吊物画虚线连到天花板（否则只是一个"浮在空中的方块"，读不出"可以勾"）<br>· **五类外形**：主角有"朝向眼"、石块有内圈、陶罐有"罐口"、墨卒有两点白眼睛；地面顶面画一条亮边（哪里能站一眼可读）<br>· **瞄准预览**：高亮目标物体 + 吸附点绿环（落空红叉）+ **射程圈**（丝是有射程的，圈外连不上）<br>· **小字标签**：我/石/罐/卒/梁/悬梁/吊桩/深沟。走 `LabelLayer`（`Label` 节点池，按 key 复用，不每帧 new Node）；**`L` 键可关**。它是灰盒读图辅助，不是玩家可见文案——设计 §8.4 的 500 字预算管的是后者，R2 做美术时整层关掉 |
| **影响面** | FR-RND-003（分辨率与坐标）/ FR-UI-001（丝位指示，**这条的"已验证"要重新看**：原来那个 ✅ 依据的是"面板正常显示"，而圆点其实一直在屏幕外）；`cocos/Coordinates.ts`、`cocos/GrayboxRenderer.ts`、新增 `cocos/LabelLayer.ts` / `cocos/labels.ts`；回归 `tests/hud.test.ts` |
| **第 14 轮加强（创始人：「我无法直观地感受到我能连接多远的物体」）** | 上一轮只画了一条 1px 细圆，太容易被忽略。现在做成三件套：**很淡的填充盘**（这一片够得着）+ **虚线边界**（边界看得清）+ **超出射程时瞄准线变红、并在边界上打叉**。同时把量程起点从主角中心改成**手部**（丝是从手出去的） |

### D-056 · 丝线对主角：**只有收丝才施力**；着地时只被挡住，不被拽起 ★

| 项 | 内容 |
|---|---|
| **状态** | 已决（第 14 轮实机反馈，创始人原话驱动） |
| **创始人原话** | 「我连接到横梁之后移动到丝线张力范围外后会被**直接拉到天上**，这显然不是我想要的效果，我希望到达张力最大之后只是我的人物**无法继续移动**（最多是由于弹性势能被回拉一点位置）而不是被拉走，**人物能被拉走的方式是收丝**」 |
| **实测根因** | 张力上限 1200 N 对 0.5 kg 的主角等于 **240 g**。主角着地时等效质量 1e6，这个力什么都做不了（所以走路时毫无感觉）；可一旦**踏空**、等效质量回到 0.5，同一根绷紧的丝在一帧内就把他推成 **vy = +29 m/s**。逐帧记录：i=27 在 x=15.4 踏空 → i=28 速度为 (−0.38, **+29.16**) → i=40 已经在 6.7 米高 |
| **修改两处** | ① **主角这一端只有"在收丝"时才吃弹簧力**（`applyForces`）；没在收丝时**一点力都不给**——注意不能给"一点点"，因为丝一旦绷紧 `L > 目标丝长` 就持续成立，哪怕 25 m/s² 一秒后也是 25 m/s，人还是会被慢慢吊上去。<br>② **着地且没在收丝时，刚性约束只夹水平方向**（`solveRopeConstraints`）：解出 `|锚点 − (x, 手部y)| = 最大丝长` 的那个 x，把人**在地上**推回可达范围，竖直方向一点不动 |
| **结果（实测）** | 连着横梁一直往右走：不再上天，而是被挡在可达范围边缘；踏空之后是**正常的摆荡**（绳长恒为 11.55、人在低空摆过沟），不是弹射 |
| **没被牺牲的东西** | **收丝仍然把人拉走**（质量差那一课的核心）✓；**甩石头的投掷手感没变**（演示脚本的断丝瞬间甩速仍是 **9.43 m/s**、墨甲仍被打掉 9.9 HP —— 说明甩动本来就由刚性约束承担，不依赖那条被拿掉的弹簧力）；着地时物体被拉过来也照旧（物体那一端仍然全额吃弹簧力） |
| **影响面** | FR-PHY-003（着地/离地等效质量）/ FR-PHY-006（到顶变刚性）/ FR-PHY-013；`core/world.ts` 两处；回归 `tests/prologue.test.ts` 的 `#4` 两条（"只挡不拽" + "收丝仍然拉得走"）；演示哈希基线更新为 `ec2bc6634a4e0ea6` |

### D-057 · 序章改成**「教明白」**：目标行 + 当前这一步 + 物体作用（**推翻设计 §7 / §8.4 的零文字**）★

| 项 | 内容 |
|---|---|
| **状态** | 已决（第 14 轮实机反馈，创始人原话驱动）；**这一条改变了 AC-01 的口径，必须让创始人知道** |
| **创始人原话** | 「我认为序章的作用是让玩家弄懂游戏机制和游戏目标：怎么收放丝线、丝线的作用是什么、怎么跨过障碍、怎么攻击敌人，但是当我试玩时**我完全不知道我该干什么**，因此序章是不是应该重新设置一下流程，场景可以不用改变，但是**加上文字提示**：介绍当前场景的物体的作用、现在这一步该干什么，**这个游戏要干什么才能通过这一关**等等」 |
| **做了什么** | 新增 `Guidance`（`goal` / `step` / `notes`）与 `PlayableScene.guidance()`：<br>· **目标行**（屏幕顶部，一直挂着）：这一关要干什么；<br>· **当前这一步**（屏幕中上方，字号最大）：现在按什么、做什么，**做完就换下一句**（牵→收→断→去砸陶罐→打墨卒→质量差→过沟）；<br>· **物体说明**（目标行下方几行）：石头/陶罐/墨卒/梁柱/轻陶罐/悬吊横梁/对岸吊桩/深沟各是干什么的，**按段出现** |
| **⚠️ 与 AC-01 的冲突，以及我的处理** | AC-01 量的是「**首次玩家**在 60 秒内**自主**用石块击杀」。一旦把答案写在屏幕上，它测的就成了"执行力"而不是"洞察力"。处理办法是**分两级 + 记时刻**：<br>· 遭遇战前 `ENCOUNTER_EXPLICIT_HINT_SEC = 20` 秒只给方向（"它朝你爬过来了，用你刚学会的三件事对付它"），之后才给完整解法（"连上石头 → 收 → 甩 → 断"）；<br>· 埋点新增 `hint_shown` 事件，**记下每句提示第一次出现的时刻**；<br>· 于是 AC-01 可以**算两次**：`ac01Seconds()`（原口径）与 `ac01UnaidedSeconds()`（**在被点破之前**就击杀）。两个数都给，谁说话都算数 |
| **代价（要写下来）** | 设计 §8.4 的「全游戏文本 ≤ 500 字」预算在这个阶段被突破（创始人明确要求）。相应地，**AC-01 不再能单独作为"玩家会不会自己想"的证据**——这正是为什么要加 `ac01UnaidedSeconds()`。R2 若要回到"零文字教学"，删掉 `Guidance` 即可，物理与关卡都不用动 |
| **影响面** | 设计 §7（序章教学手段）/ §8.4（文案预算）；FR-UI-007 / NFR-I18N-003 / AC-01；新增 `core/texts.ts` 的 `goal*` / `step*` / `note*`、`core/playable.ts` 的 `Guidance`、`cocos/Bootstrap.ts` 的三个 Label 节点；回归「目标行随段落变化」「两级提示」「hint_shown 埋点」 |

### D-058 · 靶子陶罐改为**可附着的静态靶**；墨卒加**位置硬护栏**；掉沟物件送回出生点

| 项 | 内容 |
|---|---|
| **状态** | 已决（第 14 轮实机反馈 #1、#2） |
| **创始人原话** | ①「丝线附着不上陶罐」 ②「墨卒移动会把陶罐推进沟里」 |
| **① 附着不上：不是 bug，是我上一轮的设计** | `jar` 在批 1 被写成 `anchorable: false`（当它是"弹药"）。但场上摆着**两个长得一模一样**的陶罐、其中一个怎么都射不上丝 —— 从玩家角度这就是 bug。**改为可附着** |
| **② 被推进沟里：有两个推手，不止墨卒** | 实测复现：玩家往右走 → 墨卒跟着往右，把罐子从 13 推到 **15.95 掉进沟**。给墨卒加护栏（14.5）之后**仍然掉**——因为**推土机是把东西推到前面去的**，拦住了推土机，罐子早被顶过沟沿了。再往下查，**在地上滚的石块也会推它**。<br>结论：只要罐子还是动态的，"被推过沟沿"这件事就堵不干净 → **把它做成静态靶子**（`kind: 'static'` + `damageMass: 0.6`，质量口径仍是设计 §2.6 的 0.6，做法同 D-047 的梁柱）。静态刚体**物理上不可能**被推进沟里，投掷教学也变成了固定靶 |
| **代价** | 静态的它**没有"轻"的手感**（收丝会把你拉过去，而不是把它拉过来）。质量差教学用的是 5:00 给出的那个**轻陶罐 `pot`**（仍然是动态 0.6），两者角色不同、行为不同是**有意的** |
| **墨卒护栏** | 除了"脚本给的向右速度"护栏，再加一道**位置硬护栏**（`scriptMote` 里直接把越线的 `pos` 推回来）：速度护栏挡不住**接触推挤**（实测玩家能把墨卒顶到 x=15.29 掉进沟）。护栏取 `PROLOGUE_MOTE_LEDGE_GUARD_X = 11.8`，落在陶罐（13）**左边**，这样它的行进路线碰不到任何道具 |
| **掉沟物件送回出生点** | 段落是**纯进度门控**（D-054），所以"把石块扔进沟里"= **死局**。现在 `checkFall` 一并处理非静态可动物件：送回出生点、速度归零。主角送回沟边、物件送回出生点，规则一致：**掉下去不是惩罚，只是白费一次** |
| **影响面** | 设计 §2.6（陶罐质量口径不变）；FR-LVL-005（可交互环境物体）/ FR-CBT-002（伤害管线现在也服务静态靶）；`core/scene_prologue.ts`（jar 静态化 + scriptMote + checkFall）、`core/constants.ts`（护栏 11.8 / 出生点 11.5）；`cocos/GrayboxRenderer.ts`（**静态道具要按道具画、不能当地形**）；回归 `#1` / `#2` / 「掉沟物件送回出生点」 |
| **第 15 轮修订** | 创始人随后明确要求「**陶罐就按你原来的方案设计成不可吸附吧**」⇒ 靶子改回 `anchorable: false`（我上一轮按他更早的反馈改成了可附着，被他纠正）。为了不再让人误以为能勾，标签从「罐」改成「**靶**」，而可附着的轻陶罐仍叫「罐」。同时给它加了 `fragile`（易碎）—— 见 D-059 |

### D-059 · 教学关的伤害口径：**罐子易碎 + 墨卒 HP 3 + 停距 5m**（切割门槛 v≥15 撞上了玩家实际速度）★

| 项 | 内容 |
|---|---|
| **状态** | 已决（第 15 轮实机反馈「甩出石块貌似造成不了伤害：砸不碎陶罐，墨卒也杀不死」）；**其中 HP 5→3 偏离设计附录 B，需要创始人追认** |
| **根因（算出来的，不是猜的）** | 设计 §2.5 的两道门槛对三种目标的实际后果：<br>· **墨甲**（20 质量 / 弱点 impact）：`min(4,20)=4 ≥ 3` ✓ ⇒ 冲击伤害 = v ⇒ **6 m/s 就能打**<br>· **墨卒**（1 质量 / any）：`min(4,1)=1 < 3` ⇒ **冲击永远被挡**，只剩切割，而切割门槛是 **v ≥ 15**<br>· **陶罐**（0.6 质量 / any）：`min(4,0.6)=0.6 < 3` ⇒ 同理，**要 v ≥ 15**<br>**M0 之所以从没暴露这个问题：它唯一的靶子是"impact 型"，6 m/s 就能打。** 序章一上来换成 `any` 型的轻目标，门槛立刻从 6 跳到 15 |
| **玩家实际能甩多快（网格实测）** | 对"收丝时长 × 摆动周期"跑了 30 组手法，判据是"**朝目标方向且够快**"：<br>· **v ≥ 15 → 19/30 组**（最快 0.5 秒）<br>· **v ≥ 17.3 → 8/30 组**（那才是 HP 5 的"一下死"要求）<br>· **v ≥ 20 → 0/30 组**（而伤害表是按 20 m/s 写的）<br>⇒ 正常打法 15~16 m/s，**正好卡在门槛上**：时灵时不灵，读起来就是"造成不了伤害"。<br>（顺带排除了"换成疾丝 14 m/s 会更好"：实测疾丝反而更差 —— 收得越快、绳越短，切向速度越小。） |
| **改动 ①：罐子易碎** | 新增 `Body.fragile`：达到 `MIN_DAMAGE_SPEED`（6 m/s）的撞击**当场碎掉**，不走两道门槛。<br>依据：陶罐是我在 D-044 里自己定的"可破坏场景物"，**设计里没有它的耐久口径**，所以由我定义 —— 罐子就是脆的，砸到就碎（与它自己的提示文案"砸一下就碎"一致） |
| **改动 ②：墨卒 HP 5 → 3**（⚠️ 偏离附录 B） | 15 m/s 的切割伤害 = 3.75 ⇒ **只要打出了伤害，就一定会死**。这正是附录 B 那句"一下死，它是教学工具，不是挑战"要的效果；HP 5 需要 17.3 m/s，只有 8/30 组手法做得到，会让玩家反复遇到"打中了却只掉一点血"。<br>**若创始人坚持 5**，那就得同时承认"墨卒要打两次"（改附录 B），或者把切割门槛从 15 调下来（FR-CBT-003，影响面更大） |
| **改动 ③：墨卒停距 2.5 → 5m** | 2.5 米时它会**挤进玩家的甩击空间**：石头刚出手就碰到它（低速碰撞不造成伤害，只把它推开），玩家会觉得"怎么打都没反应"。5 米既在丝的射程内（12m）、又在投掷的舒适距离上 |
| **顺带把引导改了** | `stepEncounterExplicit` 原来只写"连上石头 → 收 → 甩 → 断"，**没说速度** —— 创始人照着做但速度不够就一点伤害都没有。现在写的是"**左右来回跑、同时按住「收」把石头甩起来，看预判线 —— 甩到线指向它再按「断」**" |
| **影响面** | FR-CBT-002/003（伤害门槛的口径**未改**，改的是目标侧参数）；附录 B（墨卒 HP，**待追认**）；`core/body.ts` + `core/world.ts`（`fragile`）、`core/constants.ts`（HP / 停距）、`core/scene_prologue.ts`（罐子易碎）；回归 `#3 甩起来的一击真的能打死墨卒`（端到端） |

### D-060 · 敌人**显示血量**（推翻 FR-UI-005「不显示血条」）

| 项 | 内容 |
|---|---|
| **状态** | 已决（第 15 轮实机反馈：「敌人可以加上血量提示，让玩家能清楚的看到砸了多少血量」） |
| **与设计的冲突** | 设计 §8.4 / FR-UI-005 写的是「**不显示血条**」，用"颜色深浅 + 攻击反馈"表达血量。创始人现在明确要看得见的血量 —— **以创始人的要求为准**，FR-UI-005 需要改写（待追认） |
| **做法** | 敌人头顶画**分段血条**（一格 = 1 点血）+ **数字**（`3/3`）。<br>为什么分段而不是细长条：墨卒只有 3 点血，200px 的细条掉 1 点几乎看不出来，而玩家要的恰恰是"清楚的看到砸了多少" |
| **折中** | 满血时血条是淡的、被打过之后变醒目 —— 设计"别让血条抢戏"的精神保住一点 |
| **影响面** | FR-UI-005（待改写）；`cocos/GrayboxRenderer.ts`（`drawHpBar`）、`cocos/labels.ts`（血量数字）、`cocos/LabelLayer.ts`（节点池扩到 36） |

---

## 待决 ★

### D-007 · 商业化路径（**最高优先级**）

| 项 | 内容 |
|---|---|
| **状态** | **待决** |
| **必须决策时点** | **立项第一天** |
| **选项** | A. 纯广告（IAA） / B. 纯买断（IAP） / C. 广告 + 内购 |
| **后果** | 见下表 |
| **为什么最急** | 版号是本项目**唯一不可压缩的关键路径** |

| 路径 | 主体 | 版号 | 最快上线 |
|---|---|---|---|
| A. 纯 IAA | 个人 | **可免**（需软著 + 自审报告） | 2–4 个月 |
| B. 纯 IAP | 个人 | 需版号 | 6–12 个月 |
| C. IAA + IAP | **须企业** | 需版号 | 6–12 个月 + 企业资质 |

**若目标上线时间 < 12 个月，必须走路径 A。**

### D-008 · 主体类型

| 项 | 内容 |
|---|---|
| **状态** | 待决 |
| **必须决策时点** | 立项第一周 |
| **选项** | 个人 / 企业 |
| **影响** | 决定版号路径（D-007）、微信支付能力、私有仓库协作人数上限 |
| **备注** | 个人账号下所有私有仓库总协作人数上限 5 人 |

### D-001 · 引擎

| 项 | 内容 |
|---|---|
| **状态** | **已决**（原为待决） |
| **日期** | 2026-09（决策） |
| **决策** | **Cocos Creator 3.x**，目标版本 **3.8.x** |
| **理由** | 2D 物理 + 3D 相机视差一套代码出小游戏与 PC；微信小游戏原生支持 |
| **备选** | Unity + WebGL 转换（小游戏上体积与性能吃亏）、自研 |
| **影响面** | 全部技术需求（NFR-PERF / NFR-LOAD / NFR-COMP） |
| **执行前提** | 本机**尚未安装** Cocos Creator。创始人负责安装；在编辑器就位前，工程中所有纯逻辑模块必须保持零引擎依赖，以便在本机（Node）继续验证 |

### D-009 · 美术方案

| 项 | 内容 |
|---|---|
| **状态** | 待决 |
| **必须决策时点** | R2 之前 |
| **选项** | 外包画师 / 自建 / 采购素材库 / 程序化生成 |
| **影响** | R2 预算与周期；Gitee 仓库容量（见 D-013） |

### D-010 · 音频方案

| 项 | 内容 |
|---|---|
| **状态** | 待决 |
| **必须决策时点** | R2 之前 |
| **需求** | 古琴单弦为核心音色（NFR-AUD-001）；收丝音高随张力上升（NFR-AUD-002） |

### D-013 · 大体积素材的存放位置

| 项 | 内容 |
|---|---|
| **状态** | 待决 |
| **必须决策时点** | R2 之前 |
| **背景** | Gitee 社区版 **单仓库 500MB、单文件 50MB、账号总容量 5GB** |
| **选项** | ① 全部进 Git（不可行）② Git LFS（占用同一配额）③ 对象存储 OSS/COS + 仓库只留成品 |
| **建议** | 方案 ③。美术源文件（PSD/ASE）不进仓库，只提交导出的成品资源 |

---

## 待验证

### D-014 · 凝神（子弹时间）是否保留

| 项 | 内容 |
|---|---|
| **状态** | 待验证 |
| **验证方式** | R1 触屏实测 |
| **背景** | 凝神是「借力打力」在触屏上可实现的**必要条件**。但对准一把挥动的巨剑在触屏上极难 |
| **若砍掉** | 连锁后果：终章 BOSS 借力打力失效 → UC-03 主流程重设计 → FR-LVL-001 六章结构重排 |
| **结论** | **不可轻易砍**。要砍须同时重新设计终章 |

### D-015 · 手感参数

| 项 | 内容 |
|---|---|
| **状态** | 待验证 |
| **当前值** | 重力 g=20 m/s²、收丝 8 m/s、张力上限 400、主角质量 0.5 |
| **说明** | 这些是**起点，不是答案**。需真机反复调参 |
| **决策权** | 项目负责人拍板 |

### D-016 · 单屏房间制 vs 有限卷轴

| 项 | 内容 |
|---|---|
| **状态** | 待验证 |
| **当前倾向** | 单屏（FR-LVL-004），关卡设计难度低一个数量级 |
| **验证方式** | M1 实作 |

---

## 变更日志

| 日期 | 变更 |
|---|---|
| 2026-09 | 建立本文件；录入 D-001 ~ D-016 |
| 2026-09 | **D-001 由「待决」转为「已决」**：定为 Cocos Creator 3.x（3.8.x） |
| 2026-09 | 新增 D-017 ~ D-026（M0 开工决策）：物理内核自研、单位制、主角着地模型、丝线连接语义、张力模型、伤害择一结算、附录数值复核、预判线口径、M0 不做自转、M0 敌人行为 |
| 2026-09 | **M0 实测定标后修订**：D-021 张力常数被推翻并标记「已废弃」（教训：数值巧合 ≠ 设计意图证据）；新增 **D-027**（被牵物体不参与碰撞）、**D-028**（刚度/阻尼重新定标）、**D-029**（显示刻度 vs 物理断裂载荷，1200 N）、**D-030**（收丝机堵转）、**D-031**（投掷射程对关卡尺度的硬约束） |
| 2026-09 | **第 3 轮实机反馈后的机制修订**：**D-032 取消"超限断弦"，改为到顶变刚性**（创始人提案；FR-PHY-006 需改写，**D-030 作废**）；新增 **D-033**（脱离豁免：已在主角体内的物体不再撞主角）、**D-034**（断丝改为右键/Q，按住左键收丝，丝线加粗） |
| 2026-09 | **创始人批准 D-032 后执行 SRS 变更**：SRS 升 **v1.2**（FR-PHY-006 / AC-07 / §1.3 术语 / UC-02 异常流程已改写）；RTM §6 补入影响回溯实录；设计 §2.4 加修订说明。新增 **D-035**（主角与 prop 一律不碰撞，**取代 D-027**） |
| 2026-09 | **第 5 轮实机反馈**：新增 **D-036**（位置层约束交替迭代，修"墨甲和石块卡在一起"，穿透 0.236→0.084m）、**D-037**（攻击门槛：被牵住的物体不造成伤害 + 最小伤害速度 6m/s）、**D-038**（敌人不可被丝线附着 —— **墨丝心法第四章才解锁**，修正了我此前的实现错误） |
| 2026-09 | **第 6 轮**：新增 **D-039**（道具摩擦 0.4→0.1，补偿 M0 没有自转；投掷满伤害射程 ~9m→~15m）。**文档位置约定**：`DECISIONS.md` 与 `CHANGELOG.md` 移入 `docs/`，此后**所有文档一律放 `docs/`**，仓库根目录只留工程文件 |
| 2026-09 | **M0 判定通过**（创始人实机试玩：「试玩通过了」）。物理内核成立 —— 设计 §11.2 的**最高风险（物理手感）落地**。进入 M1（序章前 12 分钟）。**D-012（R1 阶段零美术）继续有效**：M1 与 M2 之间仍不做任何美术。M1 新增的验证目标是 **AC-01 自主解法率**（不是手感——手感已经过了） |
| 2026-09 | **M1 开工决策**：新增 **D-041**（关卡完成判定 = 穿过「门」即过关，敌人不必清空）、**D-042**（AC-01 埋点口径 = 完整时间线 + 可一键导出）、**D-043**（M1 按设计 §7 的时间段分 4 批交付） |
| 2026-09 | **M1 批 1 交付**（0:00–3:00）：新增 **D-044**（可破坏场景物走与敌人相同的伤害管线；`shattersOnDeath` / `removed` 两个字段，**不从 bodies 数组删元素以免 id 错位破坏确定性**）；新增 `core/playable.ts`（场景可插拔接口）、`core/texts.ts`（NFR-I18N-003 文案表）、`core/hints.ts`（条件驱动的极简提示） |
| 2026-09 | **M1 批 2 交付**（3:00–5:00）：新增 **D-045**（序章用进度门控而非时间轴，待追认）、**D-046**（`damage`/`killed` 事件补 `by` 攻击者字段，为 AC-01 判定服务）；新增 `core/telemetry.ts`（AC-01 完整时间线埋点，JSONL/CSV 导出） |
| 2026-09 | **M1 批 3 交付**（5:00–8:00）：新增 **D-047**（梁柱做成不可动结构——**裁定设计 §2.2 与 §7 的冲突**，取 §2.6 的定性"锚点与移动手段"；一处待创始人裁定）、**D-048**（附着与丝线锚点按"表面最近点"算，大物体必需；小物体仍取中心以保住 M0 已验收的手感） |
| 2026-09 | **M1 批 4 交付**（8:00–12:00，序章收尾）：新增 **D-049**（过沟路线定为**两步"被拉过去"**，不是荡过去——先算出摆荡在数值上不成立；顺带发现主横梁曾是"一根丝就能通关"的滑轨，已收窄）、**D-050**（**静态结构永远与主角碰撞** + 深穿透不算站得住 + 过关要求站在平台高度上——修"收丝会把人拉进横梁里以 ±25m/s 抖动"与"嵌在 12 米高处被判通关"两个真 bug）、**D-051**（序章墨卒改从**左平台内侧 x=12** 入场 + 沟沿护栏——修"敌人自己走进沟里永远下落"与"陶罐挡掉 AC-01 弹道"）。**本轮最有价值的一条**：批 4 的几何改动让前 3 批的两条既有行为（敌人巡逻、投掷弹道）当场失效，**是回归测试把它们抓出来的** |
| 2026-09 | **第 13 轮（第一次试玩反馈）**：新增 **D-052**（丝线改为"按住瞄准、松手发射，**松手点即附着点**"，并新增 `flying` 飞行过程；**锚点锁存**顺带从根上关掉 D-049 那个"横梁滑轨"漏洞；D-048 的锚点语义被取代）、**D-053**（操作方案改成**左右两个按钮 + 收/放/断三个屏幕按钮**，取消左半屏虚拟摇杆；按钮几何唯一定义在 `core/hud.ts`，输入与渲染共用）、**D-054**（段落推进改为**纯进度门控**，删掉 120s/180s 两条时间轴兜底，另加调试跳段键）、**D-055**（**修两个"看不见"的 bug**：深沟整个在屏幕外、HUD 被画在屏幕外；并重做物体可读性：深沟画成黑洞、可附着结构配色、五类外形、瞄准预览、小字标签） |
| 2026-09 | **第 14 轮（第二次试玩反馈）**：新增 **D-056**（**丝线对主角只有收丝才施力**；着地时刚性约束只夹水平方向 —— 修"连着横梁往右走会被一帧 29 m/s 拉上天"；实测甩石头的投掷手感未受影响）、**D-057**（序章改成**「教明白」**：目标行 + 当前这一步 + 物体作用，**推翻设计 §7/§8.4 的零文字教学**；为保住 AC-01，提示分两级并把每句首次出现的时刻埋点，新增 `ac01UnaidedSeconds()`）、**D-058**（靶子陶罐改为**可附着的静态靶**（原来 `anchorable:false` 导致"附着不上陶罐"）、墨卒加**位置硬护栏**（速度护栏挡不住接触推挤，罐子会被顶进沟）、**掉沟物件送回出生点**（纯进度门控下否则是死局）） |
| 2026-09 | **第 15 轮（第三次试玩反馈）**：新增 **D-059**（教学关伤害口径：罐子**易碎**、墨卒 **HP 5→3**、停距 **2.5→5m** —— 根因是切割门槛 v≥15 撞上了玩家实测的 15~16 m/s，而 M0 唯一的靶子是 impact 型所以从未暴露）、**D-060**（敌人**显示血量**，**推翻 FR-UI-005「不显示血条」**）。另修一个真 bug：`GrayboxRenderer` 漏 import 导致**瞄准期间整帧中断**（瞄准线/绿圈/红叉/射程/HUD/标签/引导一起消失），并在 `lint-architecture.mjs` 里加了**漏导入检查**防复发 |
| 2026-09 | **待创始人追认**：D-017（偏离 SRS §2.3 的 Box2D 路线）、D-018（单位与 60px/m）、D-019（着地质量 1e6）、D-023（附录数值复核结论）、D-029（张力重新定标）、D-034（输入方案调整）、D-045（序章用进度门控而非时间轴）、**D-049（第二根丝"是否必需" + 8:00–12:00 的零提示难度）**、**D-052 的"松手点即附着点"字面语义**、**D-053 带来的 FR-ACT-001/002/003 三条需求改写**、**D-057 对设计 §7/§8.4「零文字」的推翻与 AC-01 口径的调整**。（D-032 已于第 4 轮正式批准并执行 SRS 变更） |

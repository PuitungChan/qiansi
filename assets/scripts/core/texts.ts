/**
 * 《牵丝》—— 玩家可见文案表。
 *
 * NFR-I18N-003（R1）要求「文案全走资源表」。这是那张表。
 * 任何出现在屏幕上的玩家可见文字都必须从这里取，不许内联。
 *
 * 设计 §8.4：全游戏唯一允许的文字是「命名」，除此之外总量 ≤ 500 字（FR-UI-007）。
 * 所以这张表应该长得很慢——每加一句都该问一次"能不能不说话就教会"。
 *
 * ## 第 14 轮的一次重大偏离（创始人指令）
 *
 * 创始人第一次试玩后的原话：
 * 「我在思考序章的作用是什么，我认为是让玩家弄懂游戏机制和游戏目标：怎么收放丝线、
 * 丝线的作用是什么、怎么跨过障碍、怎么攻击敌人，但是当我试玩时我完全不知道我该干什么，
 * 因此序章是不是应该重新设置一下流程，场景可以不用改变，但是加上文字提示：
 * 介绍当前场景的物体的作用、现在这一步该干什么，这个游戏要干什么才能通过这一关等等」
 *
 * 于是序章从"三句话、其余全靠物理自己说话"（设计 §7 / §8.4 的原意）改成有条理的引导：
 *   · 目标行：这一关要干什么（`goal*`）
 *   · 当前这一步：现在按什么、做什么（`hint*` / `step*`）
 *   · 物体作用：场上的东西各是干什么的（`note*`）
 *
 * 代价必须记下来：字数预算（≤ 500 字）在这个阶段会被突破，
 * 且 AC-01「自主解法率」的口径被改变——玩家不再是"自己想到用石头"，而是"照着提示做"。
 * 所以埋点新增了 `hint_shown` 事件，记下每句提示第一次出现的时刻，
 * 这样 AC-01 仍然可以按"在看到完整解法之前就用石块击杀"单独统计一次（见 telemetry）。
 * 两个数都给，谁说话都算数。
 *
 * 本文件不得引入任何引擎依赖。
 */

export const TEXTS = {
  // ── ① 牵 / 收 / 断 三句（设计 §7 的三个动词，第 14 轮补上"按哪里"）──
  hintAttach: '① 牵：按住屏幕瞄准石头，松手把丝射出去',
  hintReel: '② 收：按住右下「收」（键鼠滚轮上），把石头拉起来',
  hintCut: '③ 断：按右下「断」（键鼠 Q），断丝就是攻击',

  // ── ② 各段"现在该做什么" ──────────────────────────────
  /** 教学段做完三件事之后 */
  stepBreakJar: '把石头甩向右边那个靶子（罐子），砸碎它',
  /** 遭遇战：先给一句不点破的，攒够时间再点破 */
  stepEncounterVague: '它朝你爬过来了。用你刚学会的三件事对付它',
  /**
   * 遭遇战：完整解法。**必须把"要甩起来"说清楚** —— 第 14 轮实测：
   * 墨卒质量 1 ⇒ 冲击公式被 `m_eff ≥ 3` 挡下，只剩切割，而切割门槛是 **v ≥ 15 m/s**；
   * 玩家朝目标方向的正常甩投是 15~16 m/s，**只有真甩起来才够**。
   * 只写"连上石头→收→甩→断"是不够的（创始人照着做，但速度不够 ⇒ 一点伤害都没有）。
   */
  stepEncounterExplicit: '左右来回跑、同时按住「收」把石头甩起来，看预判线 —— 甩到线指向它再按「断」',
  /** 质量差 */
  stepMassDiff: '连梁柱收丝（你会被拉上去）→ 再连轻陶罐收丝（它会飞过来）',
  /** 双丝 + 深沟 */
  stepDual:
    '连悬吊横梁 → 收丝（人被拉到沟上方）→ 连对岸吊桩 → 按「断」断掉第一根 → 收丝 → 落到对岸',
  /** 通关 */
  cleared: '过了',

  // ── ③ 每一关的目标（一直挂在屏幕上）──────────────────
  goalTutorial: '目标：学会 牵 / 收 / 断 三件事',
  goalEncounter: '目标：用石头砸死墨卒（它挡着路）',
  goalMassDiff: '目标：体会"重的把你拉过去、轻的飞过来"',
  goalDual: '目标：过深沟 —— 站到对岸平台上就算过关',
  goalCleared: '目标：已完成',

  // ── ④ 场上物体各是干什么的（按段出现）────────────────
  noteStone: '石头：能牵住、能甩出去（甩得越快伤害越高）',
  noteJar: '靶子：一砸就碎，用来练「断」（它不能牵）',
  noteMote: '墨卒：爬得很慢，3 格血 —— 但必须甩够快才打得动它',
  noteBeam: '梁柱：太重、拉不动 —— 连上它收丝，你会被拉过去',
  notePot: '轻陶罐：很轻 —— 连上它收丝，它会飞过来',
  noteSwingBeam: '悬吊横梁：挂在沟上方，是过沟的第一步',
  noteFarPost: '对岸吊桩：过沟的第二步，从横梁上够得着它',
  noteChasm: '深沟：没有跳跃，走不过去 —— 只能用丝过去',
} as const

export type TextKey = keyof typeof TEXTS

/** 取文案。集中走这里是为了将来做多语言时只有一个入口。 */
export function t(key: TextKey): string {
  return TEXTS[key]
}

/**
 * 全游戏玩家可见文本的字数统计（AC-33 用）。
 *
 * 第 14 轮起这个数会明显超过 500 —— 那是创始人明确要求的（见文件头）。
 * 它仍然被统计并断言，因为"超了多少"本身是需要被看见的信息。
 */
export function totalTextLength(): number {
  let n = 0
  for (const k of Object.keys(TEXTS) as TextKey[]) n += TEXTS[k].length
  return n
}
